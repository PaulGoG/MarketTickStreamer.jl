var documenterSearchIndex = {"docs":
[{"category":"section","location":"usage/#Usage-and-configuration","page":"Usage & Configuration","text":"Everything tunable lives in config/config.toml. Nothing operational is hardcoded, and the parser enforces exactly what the comments promise — types, enumerated choices, bounds — failing with a message that names the offending key, so a session cannot start from a configuration it could not honor.","title":"Usage and configuration"},{"category":"section","location":"usage/#Credentials","page":"Usage & Configuration","text":"Keys come from the file named by credentials.env_file — .env beside the repository root in the shipped configuration — or from the process environment, never from the configuration file itself. Copy the template and fill it in:\n\ncp .env.example .env      # then set ALPACA_API_KEY_ID and ALPACA_SECRET_KEY\nchmod 600 .env\n\nPaper-account keys work for market data; point [alpaca] trading_base at the paper endpoint, as the shipped configuration does. load_credentials! warns if the file is group- or world-readable.","title":"Credentials"},{"category":"section","location":"usage/#Entry-points","page":"Usage & Configuration","text":"Every script activates and instantiates the scripts environment itself (scripts/Project.toml: the package by path, plus CairoMakie and UnicodePlots for the figures and the dashboard plots), so a fresh clone needs no preparation, and each accepts an alternative config path as its first argument.\n\njulia --threads=auto scripts/stream.jl   # live capture\njulia scripts/backfill.jl                # historical download over the configured window\njulia scripts/compact.jl  data/raw/<session>_part001.jsonl\njulia scripts/replay.jl   data/raw/<session>_part001.jsonl\njulia scripts/visualize.jl data/raw/<session>_part001.jsonl\njulia scripts/monitor.jl                 # read-only dashboard for a running session\n\nFrom the REPL the same operations are run_stream, run_backfill, compact_raw and replay_source. The figure functions need using CairoMakie beside the package, and monitor_raw draws its plots once using UnicodePlots has run; the package itself depends on neither.","title":"Entry points"},{"category":"section","location":"usage/#[provider]","page":"Usage & Configuration","text":"Key Meaning\nname Provider adapter. One of: \"alpaca\" (US equities, credentialed), \"binance\" (crypto spot, public).\nfeed Live feed. Binance: \"trade\" or \"aggTrade\". Alpaca: one of \"iex\" (real-time, single venue, 30 symbols on the free tier), \"delayed_sip\" (consolidated tape, 15 minutes late, free), \"sip\" (real-time consolidated, paid).\n\nOn the free tier delayed_sip is the scientifically stronger choice: it is the complete consolidated tape, and a fixed 15-minute offset is irrelevant to any analysis that is not trading on it.","title":"[provider]"},{"category":"section","location":"usage/#[credentials]","page":"Usage & Configuration","text":"Key Meaning\nenv_file KEY=value file holding the API credentials, relative to the configuration file unless absolute.\n\nRelative paths are resolved against the directory of the configuration file, so a configuration can be copied into any project and keeps working. load_config() without an argument reads the repository's own config/config.toml, and is refused in an installed copy of the package, where that file is a read-only template to copy (MarketTickStreamer.DEFAULT_CONFIG).","title":"[credentials]"},{"category":"section","location":"usage/#[stream]","page":"Usage & Configuration","text":"Key Meaning\nsymbols Subscription list; at most limits.max_symbols entries.\nchannels Subset of \"trades\", \"quotes\", \"bars\". Trades only by default: quote and bar frames are parsed into Quote and Bar and delivered to the on_quote / on_bar callbacks of live_source, but neither is persisted. A quote stream carries an order of magnitude more messages than the trade stream, so storing it is a separate decision.\nrequire_market_open Query the market clock before connecting and exit if closed.\nwait_for_open When closed, sleep until the next open instead of exiting. The wait is shifted by the feed's intrinsic delay.\nstop_at_market_close Schedule a graceful stop at the session's next close, likewise shifted.\nreconnect_max_retries Reconnection attempts per disconnect before giving up. The counter resets after any connection that delivered data.\nreconnect_base_delay_s Backoff base; the delay is base * 2^attempt, jittered.\nreconnect_max_delay_s Backoff cap.\nstale_timeout_s Watchdog interval: a connection that delivers no frame for this long is severed and retried.\n\nThe watchdog exists because HTTP.jl 1.x WebSockets have no read idle timeout, so a silently dead TCP connection would otherwise block the read loop indefinitely.","title":"[stream]"},{"category":"section","location":"usage/#[storage]","page":"Usage & Configuration","text":"Key Meaning\ndata_dir Root of the data tree, relative to the configuration file, or absolute.\nraw_subdir Append-only NDJSON session files.\nprocessed_subdir Compacted per-symbol per-day files.\nflush_interval_s Sink flush interval.\nflush_max_ticks Sink flush batch-size threshold; whichever trigger comes first.\nprocessed_format \"csv\" or \"arrow\". CSV for interoperability and inspection; Arrow when size or read time demands it.\nprocessed_compression Compression of Arrow record batches: \"none\", \"zstd\" or \"lz4\"; Arrow only. Uncompressed files are memory-mapped and read lazily. \"zstd\" is about five times smaller (8 against 44 bytes per print on a liquid US equity day) and is decompressed on load.","title":"[storage]"},{"category":"section","location":"usage/#[limits]","page":"Usage & Configuration","text":"Key Meaning\nmax_session_hours Hard stop for a live session.\nmax_raw_file_mb Roll to a new raw part file beyond this size.\nchannel_capacity In-flight tick buffer; the producer blocks when full.\nmax_symbols Subscription cap.\nmin_free_disk_gb Refuse to start, and stop an active session, below this free space.\nmax_live_heap_mb Live-heap ceiling for backfill and compaction; the streaming paths check it and spill rather than exhaust memory.\nmax_open_spill_files File handles a spilling compaction keeps open at once.\ncompact_mem_fraction Share of the free RAM an in-memory compaction may claim; beyond it the run spills.\nspill_headroom Scratch space a spilling compaction requires, as a multiple of the input size.\ncompact_footprint_factor Estimated in-memory footprint per byte of input, which decides whether a file fits in memory.\n\nThese are the safety margins. They are the sole source of truth for the cutoffs — no threshold is hardcoded elsewhere in the pipeline.","title":"[limits]"},{"category":"section","location":"usage/#[replay]","page":"Usage & Configuration","text":"Key Meaning\npace \"recorded\" honors the original inter-arrival times; \"max\" emits as fast as the consumer takes them.\nspeed Time-compression factor when pacing is honored; 60.0 replays an hour in a minute.\nclock Timestamp that orders and paces the replay: \"recv\" (local receipt), \"exchange\" (the venue's own), or \"auto\" — receipt time when every record has it, exchange time otherwise. A backfilled recording has no receipt clock.","title":"[replay]"},{"category":"section","location":"usage/#[backfill]","page":"Usage & Configuration","text":"Key Meaning\nstart_date, end_date Inclusive exchange dates (America/New_York). Either an ISO date \"YYYY-MM-DD\" or a sentinel: \"today\", or \"today-<N>d\" for N calendar days back.\nfeed Historical feed, \"iex\" or \"sip\". Free accounts have full SIP history back to 2016, minus the trailing 15 minutes.\npage_limit Rows per REST page, from 1 to the provider maximum (Alpaca 10000, Binance 1000), which is also the default. A larger value is rejected: Binance clamps it without an error, and a clamped page reads as the end of the tape.\nrate_limit_sleep_s Pause between pages. The free tier allows 200 requests per minute.\nresume Skip (symbol, day) pairs already present under processed/, so an aborted download resumes instead of restarting.\n\nThe sentinels keep a committed configuration from going stale. They count calendar days, not trading days: a window may land on a weekend or a holiday and return nothing, which the session report will show as an empty capture rather than an error.","title":"[backfill]"},{"category":"section","location":"usage/#[rest]","page":"Usage & Configuration","text":"Key Meaning\nrequest_timeout_s Read timeout of a REST request.\nconnect_timeout_s Connection timeout of a REST request.\nmax_retries Retries on transient failures: 429, 5xx, timeouts and dropped connections.","title":"[rest]"},{"category":"section","location":"usage/#[quality]","page":"Usage & Configuration","text":"non_price_conditions lists, per tape, the sale-condition codes that disqualify a print from a price path. Tapes \"A\" and \"B\" are CTA-processed, \"C\" is UTP-processed, \"O\" is the OTC tape; the same character means different things across them, which is why the lists are separate. The shipped defaults are the provider's own published lists.\n\nThis is used only by price_forming, filter_price_forming and the n_price_forming column of session_report. Capture is never filtered — see Replay & Analysis Interfaces for why the distinction belongs to the analysis rather than to the recording.\n\nA print carrying several conditions is disqualified by any one of them, and a print on a tape with no list is kept. The set lands in the session sidecar with the rest of the configuration, so a result stays attributable to the eligibility rule that produced it.\n\nTo decode the codes themselves, fetch the provider's own glossary with condition_map rather than assuming a mapping.","title":"[quality]"},{"category":"section","location":"usage/#[monitor]","page":"Usage & Configuration","text":"Attach-mode terminal dashboard, read-only and opt-in.\n\nKey Meaning\nrefresh_s Refresh interval.\ntop_symbols Rows in the per-symbol bar plot.\nrate_window_s Rate-history window; at least refresh_s.","title":"[monitor]"},{"category":"section","location":"usage/#[logging]","page":"Usage & Configuration","text":"Key Meaning\nlevel \"debug\", \"info\", \"warn\" or \"error\".\nlog_to_file Tee the log to a per-session file under log_dir.\nlog_dir Log directory, relative to the configuration file, or absolute.\n\nFile logs are always flushed and are sanitized of terminal control sequences, so a killed session still leaves a readable tail.","title":"[logging]"},{"category":"section","location":"usage/#[alpaca]","page":"Usage & Configuration","text":"Endpoint roots, overridable so the suite can point the whole pipeline at an in-process mock server.\n\nKey Default\ntrading_base https://paper-api.alpaca.markets (paper keys) or https://api.alpaca.markets (live keys).\ndata_base https://data.alpaca.markets\nws_base wss://stream.data.alpaca.markets/v2","title":"[alpaca]"},{"category":"section","location":"api/#API-reference:-acquisition-and-storage","page":"Acquisition and storage","text":"The public surface, grouped by pipeline stage. Each group is generated from one source file, so everything carrying a docstring appears here. The analysis side — data quality, activity clocks, replay and the diagnostic figures — is on the next page.","title":"API reference: acquisition and storage"},{"category":"section","location":"api/#Configuration-and-credentials","page":"Acquisition and storage","text":"","title":"Configuration and credentials"},{"category":"section","location":"api/#Schema-and-timestamps","page":"Acquisition and storage","text":"Timestamps are Int64 nanoseconds since the UNIX epoch everywhere — the only representation that survives the full range of exchange precision without rounding.","title":"Schema and timestamps"},{"category":"section","location":"api/#Provider-interface","page":"Acquisition and storage","text":"A provider adapter implements this interface; the acquisition layer is written against it and never against a vendor. Two adapters ship, and they disagree on every axis the interface abstracts: Alpaca is credentialed, files its tape under the New York calendar, and closes overnight and at weekends; Binance is public, files under UTC, and never closes. A configuration is validated against the selected provider's own ProviderSpec rather than against one vendor's vocabulary.","title":"Provider interface"},{"category":"section","location":"api/#Alpaca-adapter","page":"Acquisition and storage","text":"","title":"Alpaca adapter"},{"category":"section","location":"api/#Binance-adapter","page":"Acquisition and storage","text":"Crypto spot, public market data, no credentials. The venue never closes, so the market-hours railings do not apply and the calendar is UTC.","title":"Binance adapter"},{"category":"section","location":"api/#Persistence-and-compaction","page":"Acquisition and storage","text":"","title":"Persistence and compaction"},{"category":"section","location":"api/#Session-orchestration","page":"Acquisition and storage","text":"","title":"Session orchestration"},{"category":"section","location":"api/#Live-monitoring","page":"Acquisition and storage","text":"","title":"Live monitoring"},{"category":"module","location":"api/#MarketTickStreamer","page":"Acquisition and storage","text":"MarketTickStreamer\n\nProvider-agnostic tick-by-tick market data acquisition for scientific time series research: live WebSocket streaming, historical REST backfill, append-only raw persistence, compaction to analysis-ready files, and paced replay of recorded sessions.\n\nEverything is driven by a TOML configuration (config/config.toml in a clone); credentials come from the environment file it names. Entry points: run_stream, run_backfill, replay_source, compact_raw.\n\nPlotting is optional. The diagnostic figures (session_figure, overview_figure and their save_* drivers) are implemented by a package extension that loads with CairoMakie, and the terminal plots of monitor_raw by one that loads with UnicodePlots; neither is a dependency of the package.\n\n\n\n\n\n","title":"MarketTickStreamer"},{"category":"constant","location":"api/#MarketTickStreamer.DEFAULT_CONFIG","page":"Acquisition and storage","text":"DEFAULT_CONFIG\n\nPath of the configuration shipped with the package, config/config.toml. In a clone of the repository it is the working configuration; in an installed copy it is a template to copy from, since the package directory is read-only there.\n\n\n\n\n\n","title":"MarketTickStreamer.DEFAULT_CONFIG"},{"category":"type","location":"api/#MarketTickStreamer.Config","page":"Acquisition and storage","text":"Config\n\nValidated, typed view of config/config.toml. Constructed via load_config. Field groups mirror the TOML tables; see config/config.toml for semantics.\n\n\n\n\n\n","title":"MarketTickStreamer.Config"},{"category":"method","location":"api/#MarketTickStreamer._cfg_value-Tuple{AbstractDict, AbstractString, AbstractString, Type{Bool}, Any}","page":"Acquisition and storage","text":"_cfg_value(tbl, table, key, T, default) -> T\n\nRead tbl[key], falling back to default, and require it to be of type T (one of Bool, Int, Float64, String). Throws ArgumentError naming table.key otherwise, so a mistyped value cannot reach the pipeline.\n\n\n\n\n\n","title":"MarketTickStreamer._cfg_value"},{"category":"method","location":"api/#MarketTickStreamer._reject_unknown_keys-Tuple{AbstractDict}","page":"Acquisition and storage","text":"_reject_unknown_keys(raw)\n\nCheck the parsed TOML against CONFIG_KEYS and the known provider tables. Throws ArgumentError naming the offending table or key, so a misspelt entry cannot be silently ignored in favour of a default.\n\n\n\n\n\n","title":"MarketTickStreamer._reject_unknown_keys"},{"category":"method","location":"api/#MarketTickStreamer._resolve_config_date-Tuple{AbstractString, AbstractString, Dates.Date}","page":"Acquisition and storage","text":"_resolve_config_date(spec, key, reference) -> Date\n\nResolve a configured calendar date. Accepts an ISO date (\"2026-08-12\") or a sentinel relative to reference: \"today\", or \"today-<N>d\" with N a non-negative integer number of calendar days. Sentinels keep a committed configuration from going stale; they count calendar days, not trading days, so a window may resolve onto a weekend or holiday and yield no prints. Throws ArgumentError naming key on any other value.\n\n\n\n\n\n","title":"MarketTickStreamer._resolve_config_date"},{"category":"function","location":"api/#MarketTickStreamer.load_config","page":"Acquisition and storage","text":"load_config(path = DEFAULT_CONFIG) -> Config\n\nRead and validate the TOML configuration. Relative storage.data_dir, logging.log_dir and credentials.env_file are resolved against the directory of path. Every key is checked for type and documented bounds, and unknown tables or keys are rejected, so a misspelt key cannot fall back to its default unnoticed. Throws ArgumentError naming the offending key.\n\nThe no-argument form reads the configuration shipped with the repository and suits a clone of it. In an installed copy of the package it throws an ArgumentError: there DEFAULT_CONFIG is a read-only template to copy into a project of your own.\n\n\n\n\n\n","title":"MarketTickStreamer.load_config"},{"category":"method","location":"api/#MarketTickStreamer.load_credentials!-Tuple{}","page":"Acquisition and storage","text":"load_credentials!(cfg::Config) -> (key, secret)\nload_credentials!(; env_path) -> (key, secret)\n\nLoad API credentials from the environment file (credentials.env_file of the configuration, or env_path), if it exists, into the process environment and return (key_id, secret_key). Looks for ALPACA_API_KEY_ID / ALPACA_SECRET_KEY. Throws an error listing what is missing if either is absent.\n\n\n\n\n\n","title":"MarketTickStreamer.load_credentials!"},{"category":"type","location":"api/#MarketTickStreamer.Bar","page":"Acquisition and storage","text":"Bar\n\nOne normalized aggregate bar: time_ns is the opening instant and close_ns the closing one, both in nanoseconds since the Unix epoch on the exchange clock, so a bar and the prints inside it share a time origin.\n\nFor an activity bar (tick_bars, volume_bars, dollar_bars) time_ns is the timestamp of its first print and close_ns that of the print that completed it. The duration close_ns - time_ns is then a random variable — the time the market needed to transact a fixed amount of activity — and is the observable a waiting-time study reads off an activity clock. For a venue-computed bar close_ns is the end of the venue's aggregation window.\n\nLike Quote, bars are parsed but not subscribed to by default. They are a convenience the venue computes; anything a bar reports can be derived from the prints this package records, and the derivation is reproducible whereas the venue's aggregation rules are not fully observable.\n\n\n\n\n\n","title":"MarketTickStreamer.Bar"},{"category":"type","location":"api/#MarketTickStreamer.Quote","page":"Acquisition and storage","text":"Quote\n\nOne normalized top-of-book quote: the best bid and offer as a venue reported them, with both clocks kept as for Trade — time_ns from the exchange, recv_ns from local receipt.\n\nQuotes are parsed but not subscribed to by default, and not persisted. A quote stream runs an order of magnitude above the trade stream in message count, which is a storage decision rather than a parsing one; reach them through the on_quote callback of live_source.\n\nSizes are round lots as the tape reports them, not shares.\n\n\n\n\n\n","title":"MarketTickStreamer.Quote"},{"category":"type","location":"api/#MarketTickStreamer.Trade","page":"Acquisition and storage","text":"Trade\n\nA single normalized trade (tick).\n\nFields\n\nsymbol     : ticker symbol\ntime_ns    : exchange/participant timestamp, ns since UNIX epoch (UTC)\nrecv_ns    : local wall-clock receive timestamp, ns since UNIX epoch (UTC).                The time_ns → recv_ns gap measures end-to-end latency and is                0 for records that never crossed the wire (e.g. backfilled).\nprice      : trade price\nsize       : trade size (Float64 to accommodate fractional shares/crypto)\nexchange   : exchange code (provider-specific, e.g. \"V\" = IEX)\nconditions : trade condition codes (provider-specific)\ntape       : tape identifier (\"A\"/\"B\"/\"C\" for US equities; may be empty)\nid         : provider trade id (0 when absent)\n\n\n\n\n\n","title":"MarketTickStreamer.Trade"},{"category":"method","location":"api/#MarketTickStreamer.now_ns-Tuple{}","page":"Acquisition and storage","text":"now_ns() -> Int64\n\nCurrent wall-clock time as Int64 nanoseconds since the UNIX epoch (UTC), built from microsecond-resolution system time.\n\n\n\n\n\n","title":"MarketTickStreamer.now_ns"},{"category":"method","location":"api/#MarketTickStreamer.ns_to_datetime-Tuple{Int64}","page":"Acquisition and storage","text":"ns_to_datetime(ns) -> DateTime\n\nTruncate a nanosecond epoch timestamp to a millisecond-resolution UTC DateTime (for display and coarse grouping only — not for storage).\n\n\n\n\n\n","title":"MarketTickStreamer.ns_to_datetime"},{"category":"method","location":"api/#MarketTickStreamer.ns_to_rfc3339-Tuple{Int64}","page":"Acquisition and storage","text":"ns_to_rfc3339(ns) -> String\n\nRender Int64 nanoseconds since the UNIX epoch as an RFC 3339 UTC timestamp with full nanosecond precision (lossless round-trip with rfc3339_to_ns).\n\n\n\n\n\n","title":"MarketTickStreamer.ns_to_rfc3339"},{"category":"method","location":"api/#MarketTickStreamer.rfc3339_to_ns-Tuple{AbstractString}","page":"Acquisition and storage","text":"rfc3339_to_ns(s) -> Int64\n\nParse an RFC 3339 timestamp with up to nanosecond fractional precision into Int64 nanoseconds since the UNIX epoch (UTC). Supports Z and ±hh:mm offsets. Throws ArgumentError on malformed input.\n\n\n\n\n\n","title":"MarketTickStreamer.rfc3339_to_ns"},{"category":"method","location":"api/#MarketTickStreamer.trading_date-Tuple{Int64}","page":"Acquisition and storage","text":"trading_date(ns; tz) -> Date\n\nExchange-local calendar date of a nanosecond epoch timestamp; used to bucket ticks into per-day files. tz defaults to America/New_York.\n\n\n\n\n\n","title":"MarketTickStreamer.trading_date"},{"category":"constant","location":"api/#MarketTickStreamer.KNOWN_PROVIDERS","page":"Acquisition and storage","text":"Provider names this build knows how to construct.\n\n\n\n\n\n","title":"MarketTickStreamer.KNOWN_PROVIDERS"},{"category":"type","location":"api/#MarketTickStreamer.AbstractProvider","page":"Acquisition and storage","text":"AbstractProvider\n\nSupertype for market-data provider adapters.\n\nA concrete adapter supplies provider_spec and make_provider for the name it answers to, exchange_tz, stream_protocol!, market_clock, historical_trades and its own message parsing. It overrides always_open, session_days and feed_delay_ns where the venue departs from the defaults.\n\nTwo adapters ship: providers/alpaca.jl (US equities, New York calendar, credentialed, closes overnight and at weekends) and providers/binance.jl (crypto spot, UTC calendar, public, never closes). They differ on every one of those axes, which is what keeps the interface honest.\n\n\n\n\n\n","title":"MarketTickStreamer.AbstractProvider"},{"category":"type","location":"api/#MarketTickStreamer.FatalStreamError","page":"Acquisition and storage","text":"FatalStreamError(msg)\n\nA stream error that must NOT trigger reconnection (bad credentials, connection-limit exceeded, invalid subscription). Aborts the session.\n\n\n\n\n\n","title":"MarketTickStreamer.FatalStreamError"},{"category":"type","location":"api/#MarketTickStreamer.LiveSession","page":"Acquisition and storage","text":"LiveSession\n\nHandle for a running live stream: the tick channel plus control state. Obtain via live_source; request shutdown with stop!.\n\nstop is atomic: it is written by whoever ends the session and read by the producer, the watchdog and the guard tasks, which run on other threads. The logging layer reads the same flag to mute transport-teardown noise, so the flag belongs to one session and not to the process.\n\n\n\n\n\n","title":"MarketTickStreamer.LiveSession"},{"category":"type","location":"api/#MarketTickStreamer.ProviderSpec","page":"Acquisition and storage","text":"ProviderSpec(feeds, backfill_feeds, tz, needs_credentials, max_page_limit,\n             regular_session, price_unit, size_unit)\n\nWhat the configuration layer must know about a provider before any provider object exists: which feed names it accepts, the calendar its tape is filed under, whether it needs credentials at all, the largest REST page the venue serves, and the venue's regular session.\n\nWithout this the config layer has to hard-code one vendor's vocabulary, which is how provider.feed came to be validated against Alpaca's feed names for every provider, and how a 24-hour venue would have had its days cut at midnight in New York.\n\nmax_page_limit exists because a venue may clamp an over-limit request instead of rejecting it, and a clamped page is indistinguishable from the last page of the tape.\n\nregular_session is (open, close) in exchange-local hours, (0.0, 24.0) for a venue that never closes. The multi-day figures lay their days out on it; nothing in acquisition depends on it, since session railings come from the venue's own clock.\n\nprice_unit and size_unit are what the venue's prices and sizes are denominated in, as the figure axes should name them: dollars and shares on a US equity tape, quote asset and base asset on a crypto pair.\n\n\n\n\n\n","title":"MarketTickStreamer.ProviderSpec"},{"category":"type","location":"api/#MarketTickStreamer.RestPolicy","page":"Acquisition and storage","text":"RestPolicy(request_timeout_s, connect_timeout_s, max_retries)\nRestPolicy()                 # 30 s, 10 s, 5 retries\nRestPolicy(cfg::Config)      # the `[rest]` table\n\nHow a provider's REST requests are bounded: the read timeout and the connection timeout, both in whole seconds, and the number of retries on a transient failure. Without a read timeout a request against a server that accepts the connection and then goes silent blocks its task for good, and with it a backfill.\n\n\n\n\n\n","title":"MarketTickStreamer.RestPolicy"},{"category":"method","location":"api/#MarketTickStreamer.always_open-Tuple{AbstractProvider}","page":"Acquisition and storage","text":"always_open(p::AbstractProvider) -> Bool\n\nWhether the venue never closes. true suppresses the market-hours railings: there is no open to wait for and no close to stop at, and a 24-hour venue asked for its next_close can only answer with a fiction. The session is then bounded by limits.max_session_hours alone.\n\n\n\n\n\n","title":"MarketTickStreamer.always_open"},{"category":"method","location":"api/#MarketTickStreamer.exchange_day_start_ns-Tuple{AbstractProvider, Dates.Date}","page":"Acquisition and storage","text":"exchange_day_start_ns(p::AbstractProvider, d::Date) -> Int64\n\nFirst instant of exchange date d on this provider's calendar, in ns since the epoch.\n\nA request window and the key its rows are bucketed by must come from the same clock. Building the window on UTC days while filing rows on exchange dates agrees only while the exchange sits at UTC-4, and silently loses an hour a day otherwise — the defect fixed in 0.2.0. ZonedDateTime resolves the offset per date, including across the 23- and 25-hour transition days.\n\n\n\n\n\n","title":"MarketTickStreamer.exchange_day_start_ns"},{"category":"function","location":"api/#MarketTickStreamer.exchange_tz","page":"Acquisition and storage","text":"exchange_tz(p::AbstractProvider) -> TimeZone\n\nThe calendar the provider's tape is filed under — the clock that decides which date a print belongs to. Defaults to the provider's ProviderSpec.\n\n\n\n\n\n","title":"MarketTickStreamer.exchange_tz"},{"category":"method","location":"api/#MarketTickStreamer.feed_delay_ns-Tuple{AbstractProvider}","page":"Acquisition and storage","text":"feed_delay_ns(p::AbstractProvider) -> Int64\n\nIntrinsic delay of the provider's configured feed (ns): the wall-clock lag between an exchange event and its earliest possible arrival on the wire. Zero for real-time feeds. Market-hours railings shift by this amount so a delayed session waits out the silent post-open window and captures the delayed tape tail after the close.\n\n\n\n\n\n","title":"MarketTickStreamer.feed_delay_ns"},{"category":"method","location":"api/#MarketTickStreamer.live_source-Tuple{AbstractProvider, Config}","page":"Acquisition and storage","text":"live_source(p::AbstractProvider, cfg::Config;\n            on_quote = nothing, on_bar = nothing,\n            stop = Threads.Atomic{Bool}(false)) -> LiveSession\n\nStart the live producer task. Streams ticks into session.channel until the session deadline (limits.max_session_hours), a stop! call, a fatal protocol error, or reconnection exhaustion — whichever comes first. The channel is closed on exit so downstream consumers terminate cleanly.\n\non_quote and on_bar are handed normalized Quote and Bar values when those channels are subscribed to (stream.channels); neither is by default, and neither is persisted — a quote stream carries an order of magnitude more messages than the trade stream, which is a storage decision taken separately.\n\nstop lets the caller own the session's stop flag, which run_stream shares with its logger.\n\nReconnects with jittered exponential backoff (stream.reconnect_base_delay_s * 2^attempt, capped at stream.reconnect_max_delay_s); the attempt counter resets after any connection that actually delivered data.\n\n\n\n\n\n","title":"MarketTickStreamer.live_source"},{"category":"method","location":"api/#MarketTickStreamer.make_provider-Union{Tuple{P}, Tuple{Val{P}, Config, AbstractString, AbstractString}} where P","page":"Acquisition and storage","text":"make_provider(::Val{name}, cfg, key, secret) -> AbstractProvider\n\nConstruct the provider called name from configuration and credentials. key/secret are empty strings when the spec says none are needed. Implemented per adapter.\n\nThe fallback throws rather than returning anything: a fallback with a value would widen every caller's inferred provider type to include it, and then each provider method downstream would carry a branch with no matching method — which is how JET found this the first time it was written otherwise.\n\n\n\n\n\n","title":"MarketTickStreamer.make_provider"},{"category":"method","location":"api/#MarketTickStreamer.provider_spec-Union{Tuple{Val{P}}, Tuple{P}} where P","page":"Acquisition and storage","text":"provider_spec(::Val{name}) -> ProviderSpec\n\nStatic description of the provider called name. Implemented per adapter.\n\n\n\n\n\n","title":"MarketTickStreamer.provider_spec"},{"category":"method","location":"api/#MarketTickStreamer.schedule_close_stop!-Tuple{LiveSession, Int64}","page":"Acquisition and storage","text":"schedule_close_stop!(s::LiveSession, close_ns; grace_s = 5.0) -> Task\n\nSpawn a guard task that gracefully stop!s the session once the wall clock passes close_ns (ns since epoch, e.g. the market's next_close) plus grace_s. Without this, a streamer left unattended sits on a silent overnight connection until the session deadline.\n\n\n\n\n\n","title":"MarketTickStreamer.schedule_close_stop!"},{"category":"method","location":"api/#MarketTickStreamer.session_days-Tuple{AbstractProvider, Dates.Date, Dates.Date}","page":"Acquisition and storage","text":"session_days(p::AbstractProvider, start_date, end_date) -> Vector{Date}\n\nThe dates in [start_date, end_date] on which the venue trades, in order.\n\nDefaults to every calendar date. A venue that rests must opt out, never the reverse: requesting a day the venue was shut costs one empty response, while skipping a day it traded loses that day's tape silently — which is what a hard-coded weekday filter did to a 24-hour venue before this was dispatched.\n\n\n\n\n\n","title":"MarketTickStreamer.session_days"},{"category":"method","location":"api/#MarketTickStreamer.stop!-Tuple{LiveSession}","page":"Acquisition and storage","text":"stop!(s::LiveSession)\n\nSignal graceful shutdown: sets the stop flag and closes the underlying WebSocket, which unblocks the producer loop; the tick channel then closes, letting sinks drain and finish.\n\n\n\n\n\n","title":"MarketTickStreamer.stop!"},{"category":"function","location":"api/#MarketTickStreamer.stream_protocol!","page":"Acquisition and storage","text":"stream_protocol!(ch, p::AbstractProvider, cfg, s::LiveSession;\n                 on_quote = nothing, on_bar = nothing)\n\nProvider interface: run one connect→auth→subscribe→stream cycle, pushing normalized Trades into ch. Must return on orderly close, throw FatalStreamError on non-retryable protocol errors, and any other exception on retryable transport failures. Implemented per provider (see providers/alpaca.jl).\n\non_quote and on_bar, when given, receive normalized Quote and Bar values for the corresponding frames. Neither channel is subscribed to by default, so neither callback fires unless stream.channels asks for it.\n\n\n\n\n\n","title":"MarketTickStreamer.stream_protocol!"},{"category":"type","location":"api/#MarketTickStreamer.AlpacaProvider","page":"Acquisition and storage","text":"AlpacaProvider\n\nConnection descriptor for Alpaca Markets: credentials, feed selection and endpoint roots (overridable via the [alpaca] config table, which lets the test suite point the client at a local mock server). rest bounds its REST requests (RestPolicy).\n\n\n\n\n\n","title":"MarketTickStreamer.AlpacaProvider"},{"category":"method","location":"api/#MarketTickStreamer.condition_map-Tuple{AlpacaProvider}","page":"Acquisition and storage","text":"condition_map(p; ticktype = \"trade\", tape = \"A\") -> Dict{String,String}\n\nFetch the provider's own sale-condition decoder from /v2/stocks/meta/conditions/{ticktype}: a map of condition code to description for one tape (\"A\", \"B\", \"C\"). ticktype is \"trade\" or \"quote\".\n\nThe same character carries different meanings on different tapes, and a vendor is free to re-map the raw CTA and UTP codes — this one delivers them unchanged — so the glossary is fetched from the provider that produced the data rather than transcribed into this package. What is held here is the much smaller NON_PRICE_CONDITIONS table of the codes that disqualify a print from a price path.\n\nRequires network access; call it once and cache the result.\n\n\n\n\n\n","title":"MarketTickStreamer.condition_map"},{"category":"method","location":"api/#MarketTickStreamer.historical_trade_count-Tuple{AlpacaProvider, AbstractString, AbstractString, AbstractString}","page":"Acquisition and storage","text":"historical_trade_count(p, symbol, start_str, end_str;\n                       feed = \"sip\", page_limit = 10_000,\n                       rate_sleep_s = 0.35) -> Int\n\nCount trades on the historical tape for symbol over the inclusive RFC 3339 window [start_str, end_str] without retaining them — the reference side of live-capture coverage checks.\n\n\n\n\n\n","title":"MarketTickStreamer.historical_trade_count"},{"category":"method","location":"api/#MarketTickStreamer.historical_trades-Tuple{AlpacaProvider, AbstractString, Dates.Date, Dates.Date}","page":"Acquisition and storage","text":"historical_trades(p, symbol, start_date, end_date;\n                  feed = \"sip\", page_limit = 10_000, rate_sleep_s = 0.35,\n                  on_page = nothing, each_page = nothing)\n\nDownload all trades for symbol in [start_date, end_date] (inclusive, exchange dates) from /v2/stocks/{symbol}/trades, following pagination tokens until exhausted. rate_sleep_s throttles between pages (free tier: 200 requests/min). Backfilled records get recv_ns = 0 — they never crossed the wire. on_page(n_page, n_total) is called per page for progress.\n\nBy default all trades are accumulated and returned as a Vector{Trade}. With each_page set, each page's trades are handed to each_page(::Vector{Trade}) instead and only the total row count is returned — memory stays bounded by one page regardless of the range.\n\n\n\n\n\n","title":"MarketTickStreamer.historical_trades"},{"category":"method","location":"api/#MarketTickStreamer.market_clock-Tuple{AlpacaProvider}","page":"Acquisition and storage","text":"market_clock(p) -> (; is_open, next_open, next_close)\n\nQuery /v2/clock on the trading API. Timestamps are returned as the raw RFC 3339 strings Alpaca sends (display only — nothing downstream computes with them).\n\n\n\n\n\n","title":"MarketTickStreamer.market_clock"},{"category":"method","location":"api/#MarketTickStreamer.subscribe_payload-Tuple{AlpacaProvider, Vector{String}, Vector{String}}","page":"Acquisition and storage","text":"subscribe_payload(p, symbols, channels) -> String\n\nBuild the JSON subscription message for the requested channels (\"trades\"/\"quotes\"/\"bars\"), e.g. {\"action\":\"subscribe\",\"trades\":[\"AAPL\",\"MSFT\"]}.\n\n\n\n\n\n","title":"MarketTickStreamer.subscribe_payload"},{"category":"type","location":"api/#MarketTickStreamer.BinanceProvider","page":"Acquisition and storage","text":"BinanceProvider\n\nConnection descriptor for Binance spot market data. Endpoint roots are overridable through the [binance] config table, which is how the test suite points the client at a local mock server.\n\nfeed selects the trade granularity: \"trade\" is every execution, \"aggTrade\" aggregates executions that filled at one price from a single taker order. They are different populations — see historical_trades. rest bounds its REST requests (RestPolicy).\n\n\n\n\n\n","title":"MarketTickStreamer.BinanceProvider"},{"category":"method","location":"api/#MarketTickStreamer.historical_trades-Tuple{BinanceProvider, AbstractString, Dates.Date, Dates.Date}","page":"Acquisition and storage","text":"historical_trades(p::BinanceProvider, symbol, start_date, end_date;\n                  feed = \"aggTrade\", page_limit = 1000, rate_sleep_s = 0.1,\n                  on_page = nothing, each_page = nothing)\n\nDownload aggregate trades for symbol over [start_date, end_date] (inclusive UTC dates) from /api/v3/aggTrades. Signature and semantics match the Alpaca adapter's, so the backfill pipeline is provider-agnostic: each_page streams pages and returns only the row count, otherwise every trade is accumulated.\n\nPagination is by trade id, not by time window. Binance rejects a startTime/endTime pair spanning an hour or more, so the range is seeded with a single startTime request and then walked forward with fromId = last id + 1 until a row's timestamp passes the end of the range. Walking hour-wide windows instead would issue 24 requests a day and still truncate any hour holding more than page_limit trades, silently. page_limit above the venue maximum of 1000 is rejected here, because the server would clamp it silently and the short-page termination rule would then end the walk after the first page.\n\naggTrade is not the tape. Binance aggregates executions that filled at one price from one taker order into a single row, so an aggregate trade is a taker order's fill, not an execution. Counts and inter-arrival times are therefore not comparable with a raw trade stream, and the choice belongs to the analysis — as with odd lots on an equity tape. Only aggTrade has a time-seekable public endpoint, which is why it is the backfill feed.\n\n\n\n\n\n","title":"MarketTickStreamer.historical_trades"},{"category":"method","location":"api/#MarketTickStreamer.market_clock-Tuple{BinanceProvider}","page":"Acquisition and storage","text":"market_clock(p::BinanceProvider) -> (; is_open, next_open, next_close)\n\nAlways open. next_open and next_close are empty because the venue has neither; always_open is true for this provider, so the session orchestration never asks for them.\n\n\n\n\n\n","title":"MarketTickStreamer.market_clock"},{"category":"method","location":"api/#MarketTickStreamer.ws_url-Tuple{BinanceProvider, Vector{String}}","page":"Acquisition and storage","text":"ws_url(p::BinanceProvider, symbols) -> String\n\nCombined-stream URL for symbols. Binance subscribes through the URL rather than through a post-connection message, so there is no auth or subscribe handshake: the connection is the subscription.\n\n\n\n\n\n","title":"MarketTickStreamer.ws_url"},{"category":"constant","location":"api/#MarketTickStreamer.SPILL_HEADROOM","page":"Acquisition and storage","text":"SPILL_HEADROOM\n\nDefault multiple of the input size that the spill filesystem must have free before compact_raw will stream to it (spill_headroom).\n\nThe spill pass trades memory for scratch space, writing every input line back out once. tempdir() is the wrong home for that: on systemd distributions /tmp is a tmpfs sized at half of RAM, so spilling there writes the copy into RAM and, on an input larger than that, takes the machine down — the failure this guard exists to prevent. The default scratch location is therefore the directory holding out_dir, and the space is verified before the first line is read.\n\n\n\n\n\n","title":"MarketTickStreamer.SPILL_HEADROOM"},{"category":"type","location":"api/#MarketTickStreamer.RawSink","page":"Acquisition and storage","text":"RawSink\n\nAppend-only NDJSON writer with size-based file rolling. Create with open_raw_sink; feed via write_batch!; always close_sink! (final flush) on shutdown.\n\n\n\n\n\n","title":"MarketTickStreamer.RawSink"},{"category":"method","location":"api/#MarketTickStreamer._safesave-Tuple{Any, AbstractString}","page":"Acquisition and storage","text":"_safesave(write_fn, path) -> String\n\nWrite a file through write_fn(tmp_path) so that path always holds the newest complete result and nothing already there is lost.\n\nThe content goes to <base>.partial<ext> in the same directory first (the extension is kept last because writers such as FileIO.save infer the format from it). If path already exists it is then preserved as the first free <base>_#N<ext>, N from 1 — a hard link where the filesystem allows one, a copy otherwise — and finally the partial file is renamed onto path. The rename replaces atomically on one filesystem, so at every instant path is either the previous complete file or the new complete one, never absent and never half-written. Backups are numbered in order of displacement: _#1 is the oldest.\n\nIf write_fn throws, the partial file is removed, path is left untouched and the exception propagates.\n\nThese are the semantics of DrWatson's safesave — newest data canonical, prior results kept as numbered backups — without the dependency.\n\n\n\n\n\n","title":"MarketTickStreamer._safesave"},{"category":"method","location":"api/#MarketTickStreamer.close_sink!-Tuple{RawSink}","page":"Acquisition and storage","text":"close_sink!(s::RawSink)\n\nFlush and close the sink's file handle.\n\n\n\n\n\n","title":"MarketTickStreamer.close_sink!"},{"category":"method","location":"api/#MarketTickStreamer.compact_raw-Tuple{AbstractVector{<:AbstractString}, AbstractString}","page":"Acquisition and storage","text":"compact_raw(raw_paths, out_dir; format = \"csv\", dedup = true,\n            mem_fraction = 0.5, tz = tz\"America/New_York\",\n            max_live_heap_mb = nothing, scratch_dir = nothing,\n            footprint_factor = 4.0, spill_headroom = SPILL_HEADROOM,\n            max_open_spill_files = 256, compression = nothing) -> Vector{String}\n\nCompact raw NDJSON files into per-symbol, per-trading-day analysis files (out_dir/SYMBOL/YYYY-MM-DD.csv|.arrow), sorted by time_ns. An existing output is never destroyed: the new file takes the canonical name and the one it displaces is kept as a numbered backup YYYY-MM-DD_#N.csv|.arrow (safesave semantics, see _safesave). Returns the list of files written.\n\ndedup = true drops exact duplicate prints (reconnection double-delivery, overlapping backfill/live captures) via deduplicate_trades, logging the count. Small inputs are compacted in memory; when the estimated footprint exceeds mem_fraction of currently free RAM the input is instead streamed line-by-line into per-(symbol, day) spill files and each group is compacted independently — memory stays bounded by the largest single group. max_live_heap_mb additionally enforces the configured heap ceiling per group (check_live_heap).\n\nThe spill pass writes a verbatim copy of the input, so it needs scratch space of the input's own size. scratch_dir places it; the default is the directory containing out_dir, not tempdir() — see SPILL_HEADROOM. The free space is checked up front and compaction refuses to start without it.\n\nfootprint_factor is the estimated in-memory size of the parsed input per input byte, spill_headroom the scratch space required as a multiple of the input size, and max_open_spill_files the number of spill files held open at once: further groups are served by closing the least recently written file and reopening it in append mode, so the pass works within the process's file-descriptor limit however many symbol-days the input spans. The pipeline takes all of them from [limits].\n\nArrow output stores the symbol, exchange, conditions and tape columns dictionary-encoded, which costs nothing to read and takes a liquid US equity day from 65 to 44 bytes per print. compression (:zstd or :lz4, Arrow only) compresses the record batches as well — 8 bytes per print with :zstd on the same day — at the price of memory-mapped reads: an uncompressed file is mapped and touched lazily, a compressed one is decompressed on load (64 ms against under 1 ms for that day). Uncompressed is the default.\n\n\n\n\n\n","title":"MarketTickStreamer.compact_raw"},{"category":"method","location":"api/#MarketTickStreamer.compact_raw-Tuple{Config, AbstractVector{<:AbstractString}}","page":"Acquisition and storage","text":"compact_raw(cfg::Config, raw_paths; dedup = true, scratch_dir = nothing)\n    -> Vector{String}\n\nCompact into cfg.processed_dir with everything else taken from the configuration: format, the provider's calendar, the heap ceiling and the spill thresholds of [limits]. This is the form the pipeline and the scripts use, so a capture is never filed under a calendar other than its provider's.\n\n\n\n\n\n","title":"MarketTickStreamer.compact_raw"},{"category":"method","location":"api/#MarketTickStreamer.json_to_trade-Tuple{AbstractString}","page":"Acquisition and storage","text":"json_to_trade(line) -> Trade\n\nParse one NDJSON line written by trade_to_json back into a Trade.\n\n\n\n\n\n","title":"MarketTickStreamer.json_to_trade"},{"category":"method","location":"api/#MarketTickStreamer.open_raw_sink-Tuple{AbstractString, AbstractString}","page":"Acquisition and storage","text":"open_raw_sink(dir, prefix; max_mb = 1024) -> RawSink\n\nOpen a fresh raw NDJSON file dir/prefix_partNNN.jsonl. Existing files are never reopened or overwritten — a new part number is chosen past any that already exist.\n\n\n\n\n\n","title":"MarketTickStreamer.open_raw_sink"},{"category":"method","location":"api/#MarketTickStreamer.processed_files-Tuple{AbstractString, AbstractString}","page":"Acquisition and storage","text":"processed_files(processed_dir, symbol; from = nothing, to = nothing)\n    -> Vector{String}\n\nThe per-day files of symbol in a processed tree (processed_dir/SYMBOL/YYYY-MM-DD.csv|.arrow), in date order, optionally restricted to [from, to]. Safesave backups (_#N) and partial files are not listed: the base file of a day is the newest and the only one a reader should open. Throws an ArgumentError when the symbol has no directory.\n\nreplay_source(processed_files(cfg.processed_dir, \"AAPL\"; from = Date(2026, 5, 1)))\n\n\n\n\n\n","title":"MarketTickStreamer.processed_files"},{"category":"method","location":"api/#MarketTickStreamer.read_raw-Tuple{AbstractVector{<:AbstractString}}","page":"Acquisition and storage","text":"read_raw(paths) -> Vector{Trade}\n\nLoad one or more raw NDJSON files into memory, skipping (and counting via a @warn) corrupt lines — e.g. a partial last line after a hard kill.\n\n\n\n\n\n","title":"MarketTickStreamer.read_raw"},{"category":"method","location":"api/#MarketTickStreamer.run_sink!-Tuple{Channel{Trade}, RawSink}","page":"Acquisition and storage","text":"run_sink!(ch::Channel{Trade}, sink::RawSink;\n          flush_interval_s = 30.0, flush_max_ticks = 5000,\n          on_flush = nothing) -> Int\n\nConsumer loop: drain ch into an in-memory batch and persist whenever the batch reaches flush_max_ticks or flush_interval_s elapses with pending data. Returns the total number of trades written. Terminates (after a final flush) once ch is closed and fully drained — closing the channel is the shutdown signal, so no locks or flags are needed.\n\non_flush(n_batch, n_total) is called after each disk write (for logging).\n\n\n\n\n\n","title":"MarketTickStreamer.run_sink!"},{"category":"method","location":"api/#MarketTickStreamer.trade_to_json-Tuple{Trade}","page":"Acquisition and storage","text":"trade_to_json(t::Trade) -> String\n\nSerialize a Trade to a single NDJSON line (no trailing newline). Timestamps stay Int64 nanoseconds — the round-trip through json_to_trade is lossless.\n\n\n\n\n\n","title":"MarketTickStreamer.trade_to_json"},{"category":"method","location":"api/#MarketTickStreamer.write_batch!-Tuple{RawSink, AbstractVector{Trade}}","page":"Acquisition and storage","text":"write_batch!(s::RawSink, trades) -> RawSink\n\nAppend a batch of trades as NDJSON lines and flush the stream once (buffered writes reach the OS per batch, not per line). Rolls to a new part file when the size limit is exceeded.\n\n\n\n\n\n","title":"MarketTickStreamer.write_batch!"},{"category":"method","location":"api/#MarketTickStreamer.acquire_session_lock-Tuple{AbstractString}","page":"Acquisition and storage","text":"acquire_session_lock(data_dir) -> lock\n\nOne session per data tree: takes a PID-file lock at <data_dir>/.session.lock and throws with a precise message if another live process already holds it (observed failure mode: two backfills interleaving on one raw directory). Stale locks from dead processes are broken automatically; release with close(lock).\n\n\n\n\n\n","title":"MarketTickStreamer.acquire_session_lock"},{"category":"method","location":"api/#MarketTickStreamer.finalize_session_meta-Tuple{AbstractString, AbstractString}","page":"Acquisition and storage","text":"finalize_session_meta(path, status; ticks, raw_files) -> String\n\nRewrite the running sidecar with the final status (\"completed\", \"interrupted\" or \"failed\"), tick count, file list, and finish timestamp. The sidecar is a mutable status record by design — the safesave rule protects data products, not status metadata.\n\n\n\n\n\n","title":"MarketTickStreamer.finalize_session_meta"},{"category":"method","location":"api/#MarketTickStreamer.reconcile_sessions!-Tuple{AbstractString}","page":"Acquisition and storage","text":"reconcile_sessions!(raw_dir) -> Int\n\nCrash-only startup reconciliation: sidecars still marked running whose recorded process no longer exists on this host are relabeled aborted (their raw NDJSON remains valid to the last flushed line); zero-byte raw stubs are reported. Never deletes anything. Returns the number of sidecars relabeled.\n\n\n\n\n\n","title":"MarketTickStreamer.reconcile_sessions!"},{"category":"method","location":"api/#MarketTickStreamer.run_backfill-Tuple{Config}","page":"Acquisition and storage","text":"run_backfill(cfg::Config; provider = nothing) -> Vector{String}\n\nDownload historical trades for every configured symbol over [backfill.start_date, backfill.end_date] (weekends skipped), persist them through the same raw-NDJSON + compaction path as live data (marked by recv_ns = 0), and return the processed file paths.\n\nWork is done one (symbol, day) at a time: its own raw part set, compacted as soon as that day finishes. Pages are flushed to disk as they arrive, so RAM stays bounded by one page and limits.max_live_heap_mb.\n\nThat granularity is what makes backfill.resume mean anything on a long download. The skip test asks whether a day is already present under processed/, so a day must land there the moment it is complete and not before: a run killed after eight of twelve hours resumes at hour eight. Compacting only at the end — as this did until 2026-09-13 — left a killed run with raw data that resume could not see, and the rerun started from nothing.\n\nAn interrupted day is not compacted and is therefore downloaded again; its partial raw file is left on disk rather than deleted, and compaction deduplicates if it is ever folded in. Ctrl-C finalizes the provenance sidecar as interrupted and returns the days that did complete. Any other error finalizes it as failed and propagates; the days compacted before the error remain valid and a rerun resumes after them.\n\nLogging is scoped to the call: the session runs under its own logger and the caller's global logger is untouched.\n\n\n\n\n\n","title":"MarketTickStreamer.run_backfill"},{"category":"method","location":"api/#MarketTickStreamer.run_entrypoint-Tuple{Function}","page":"Acquisition and storage","text":"run_entrypoint(main)\n\nRun a command-line entry point's main with clean interrupt handling: SIGINT arrives as an InterruptException and exits without a stack trace.\n\nDelivery is best-effort under threads — the crash-only session lifecycle, not this wrapper, is what actually guarantees that an interrupted run leaves recoverable state. Shared by the scripts under scripts/ and the worked examples so that every entry point behaves the same way at the terminal.\n\nExample\n\nrun_entrypoint(() -> main(ARGS))\n\n\n\n\n\n","title":"MarketTickStreamer.run_entrypoint"},{"category":"method","location":"api/#MarketTickStreamer.run_stream-Tuple{Config}","page":"Acquisition and storage","text":"run_stream(cfg::Config; provider = nothing) -> NamedTuple\n\nRun one complete live capture session:\n\noptional market-clock gate (stream.require_market_open),\nlive WebSocket source → bounded channel,\nbatched raw NDJSON persistence,\ngraceful shutdown on Ctrl-C, session deadline, or stream termination.\n\nReturns (; ticks, raw_files). Blocks until the session ends. Logging is scoped to the call: the session runs under its own logger and the caller's global logger is untouched.\n\n\n\n\n\n","title":"MarketTickStreamer.run_stream"},{"category":"method","location":"api/#MarketTickStreamer.session_id-Tuple{Config}","page":"Acquisition and storage","text":"session_id(cfg::Config; feed = cfg.feed) -> String\n\nUnique identifier for a capture session, <provider>_<feed>_<yyyymmdd-HHMMSS> (UTC start time). Prefixes every raw part file, provenance sidecar, and log file the session produces.\n\n\n\n\n\n","title":"MarketTickStreamer.session_id"},{"category":"method","location":"api/#MarketTickStreamer.setup_logging-Tuple{Config}","page":"Acquisition and storage","text":"setup_logging(cfg; session_id, shutting_down = Threads.Atomic{Bool}(false))\n    -> (logger, io)\n\nConsole logger at cfg.log_level, optionally teed to cfg.log_dir/<session_id>.log (plain formatting, no ANSI, flushed per record). Returns the logger and the open log file, or nothing without file logging; the caller runs the session under with_logger(logger) and closes io when it ends. Nothing is installed globally, so a session leaves the caller's logger as it found it and two sessions in one process keep separate logs.\n\nWhile shutting_down[] is true, records from HTTP.jl internals are dropped: during a deliberate shutdown an EOFError from a socket we closed ourselves is expected, not an incident.\n\n\n\n\n\n","title":"MarketTickStreamer.setup_logging"},{"category":"method","location":"api/#MarketTickStreamer.start_session_meta-Tuple{Config, AbstractString}","page":"Acquisition and storage","text":"start_session_meta(cfg, sid; started_utc) -> String\n\nWrite the sidecar at session START with status = \"running\" — an abruptly killed process still leaves its provenance on disk.\n\n\n\n\n\n","title":"MarketTickStreamer.start_session_meta"},{"category":"method","location":"api/#MarketTickStreamer.tee-Tuple{Channel{Trade}, Integer}","page":"Acquisition and storage","text":"tee(src::Channel{Trade}, n; capacity = 10_000, lossy = falses(n),\n    on_drop = nothing) -> Vector{Channel{Trade}}\n\nFan one tick stream out to n independent consumers (e.g. persistence plus real-time analysis taps). Outputs close when src closes.\n\nPer-output overflow policy: a non-lossy output blocks the fan-out when full (backpressure — data is never silently dropped; use for persistence, which must always win). A lossy output drops the incoming tick instead when its buffer is full (use for analysis taps that must never stall the capture); drops are counted, reported via on_drop(output_index, n_dropped) when given, and warned on first occurrence.\n\nA consumer that closes its output is dropped from the fan-out with a warning and the other outputs keep receiving, so an analysis tap that dies cannot end the persistence branch. If every output is closed the fan-out stops with an error record. A failure of the fan-out task itself is logged (errormonitor) rather than lost.\n\n\n\n\n\n","title":"MarketTickStreamer.tee"},{"category":"method","location":"api/#MarketTickStreamer.write_session_meta-Tuple{Config, AbstractString}","page":"Acquisition and storage","text":"write_session_meta(cfg, sid; status, ticks, raw_files, started_utc,\n                   finished_utc = nothing) -> String\n\nPersist a provenance sidecar <raw_dir>/<sid>.meta.toml next to the session's raw files: session summary (id, status, pid, span, tick count, file list), provenance (git commit + dirty flag, Julia and package versions, hostname, and the base name of a copy of the resolved Manifest.toml written alongside as <sid>.manifest.toml), a hardware fingerprint (CPU model and logical core count, total memory, Julia and BLAS thread counts, full versioninfo output), and the full effective configuration snapshot. The crash-only lifecycle is start_session_meta → finalize_session_meta, reconciled at startup by reconcile_sessions!.\n\n\n\n\n\n","title":"MarketTickStreamer.write_session_meta"},{"category":"type","location":"api/#MarketTickStreamer.MonitorState","page":"Acquisition and storage","text":"MonitorState\n\nIncremental tail state over one session's raw part files: per-file byte offsets and partial-line carries, cumulative per-symbol counters, a tick-rate history window, and the latest exchange timestamp seen (the tape head). Counters cover data ingested since attach (see from_start).\n\n\n\n\n\n","title":"MarketTickStreamer.MonitorState"},{"category":"method","location":"api/#MarketTickStreamer.latest_session_prefix-Tuple{AbstractString}","page":"Acquisition and storage","text":"latest_session_prefix(dir) -> String\n\nPrefix (session id) of the most recently modified raw part file in dir. Throws if none exist.\n\n\n\n\n\n","title":"MarketTickStreamer.latest_session_prefix"},{"category":"method","location":"api/#MarketTickStreamer.monitor_raw-Tuple{AbstractString}","page":"Acquisition and storage","text":"monitor_raw(dir; session = nothing, refresh_s = 2.0, top_symbols = 10,\n            rate_window_s = 300.0, from_start = false,\n            iterations = nothing, io = stdout) -> MonitorState\n\nAttach a live in-terminal dashboard to the session whose raw NDJSON files live under dir (most recently active session unless session gives an id prefix). Tails the part files read-only — safe to run beside a live capture or backfill. Renders every refresh_s: tick totals and rate, tape-head timestamp and lag, and per-symbol counts. With UnicodePlots loaded (using UnicodePlots) the counts become a bar chart and a rate-history plot is added; without it the dashboard is plain text.\n\nfrom_start = true ingests the whole existing file first (session totals; costs one full read of the raw data); the default starts at the current end of file (activity since attach). iterations bounds the number of refreshes (nothing = run until interrupted). Returns the final MonitorState.\n\n\n\n\n\n","title":"MarketTickStreamer.monitor_raw"},{"category":"section","location":"architecture/#MarketTickStreamer-—-Design","page":"Architecture","text":"Goal: acquire tick-by-tick (trade-level) market data — live and historical — as the substrate for scientifically testing exotic time series analysis methods on a real-time data flux. This document records the architecture and the reasoning behind each decision; the README covers usage.","title":"MarketTickStreamer — Design"},{"category":"section","location":"architecture/#1.-Data-flow","page":"Architecture","text":"                       ┌────────────────────────────────────────────────────┐\n config/config.toml ──▶│                 MarketTickStreamer                 │\n .env (credentials) ──▶│                                                    │\n                       │   AbstractProvider  (Alpaca today; adapter per     │\n                       │        │            provider, multiple dispatch)   │\n                       │        ▼                                           │\n                       │   ┌─ live_source ──── WebSocket, reconnect,        │\n                       │   │                   watchdog, session deadline   │\n                       │   ├─ replay_source ── recorded NDJSON, paced       │\n                       │   └─ historical_trades ── REST, paginated          │\n                       │        │                                           │\n                       │        ▼   Channel{Trade}  (bounded, typed)        │\n                       │   ┌────┴────── tee (fan-out, lossless) ───────┐    │\n                       │   ▼                                           ▼    │\n                       │  run_sink! ─▶ data/raw/*.jsonl      analysis taps  │\n                       │  (batched, append-only)             (future)       │\n                       │        │                                           │\n                       │        ▼ compact_raw                               │\n                       │  data/processed/SYMBOL/DATE.csv|.arrow             │\n                       └────────────────────────────────────────────────────┘\n\nThe load-bearing abstraction is Channel{Trade}: live, replay, and (via a trivial wrapper) backfill all produce the same typed stream, so a real-time analysis consumer developed against replay_source runs unchanged against the live feed. Backpressure is intrinsic — bounded channels block producers instead of dropping or buffering unboundedly.","title":"1. Data flow"},{"category":"section","location":"architecture/#2.-Canonical-schema","page":"Architecture","text":"Trade(symbol, time_ns, recv_ns, price, size, exchange, conditions, tape, id) — an immutable struct with concrete field types, provider-normalized.\n\nTimestamps are Int64 nanoseconds since the UNIX epoch (UTC). Dates.DateTime is millisecond-resolution and would silently truncate the nanosecond RFC 3339 timestamps Alpaca sends; a custom parser (rfc3339_to_ns) round-trips losslessly (tested at every fractional width). This representation maps directly onto Arrow Timestamp(ns) and DuckDB TIMESTAMP_NS later.\nTwo clocks per tick: time_ns (exchange event time) and recv_ns (local receipt). Their difference measures pipeline latency; replay pacing uses recv_ns (the flux as experienced); recv_ns = 0 marks backfilled rows.","title":"2. Canonical schema"},{"category":"section","location":"architecture/#3.-Persistence:-two-layers","page":"Architecture","text":"Raw — append-only NDJSON, one Trade per line, session-stamped filenames with size-based part rolling, never reopened or overwritten. Chosen over binary formats for crash-tolerance (a torn final line loses one tick, and read_raw skips it with a warning — tested) and human-inspectability. Writes are batched (flush_max_ticks / flush_interval_s) to keep I/O off the hot path.\nProcessed — per-symbol, per-trading-day (America/NewYork calendar) CSV or Arrow, time-sorted, produced by `compactraw. CSV is the default per project convention; Arrow is one config switch away when volume demands it. A rewrite never destroys a result: the new file takes the canonical name and the file it displaces is kept as a numbered backup,#1,#2, …, so the canonical path is always the newest data. These are the semantics of DrWatson'ssafesave, implemented in a dozen lines instead of taken as a dependency, because DrWatson would bring JLD2 and FileIO's loader stack into a library whose processed formats are CSV and Arrow. The write goes to a.partial` file first and is renamed into place, so a reader never sees a half-written day.\n\nScale-up path (not yet needed at a-few-symbols IEX volume): swap the raw layer to Arrow.append on stream-format files and query with DuckDB.jl — the Trade-channel interface isolates that change to sinks.jl.","title":"3. Persistence: two layers"},{"category":"section","location":"architecture/#4.-Concurrency-model","page":"Architecture","text":"Plain Julia tasks, no locks. The producer (WebSocket read loop) put!s into the bounded channel; the sink task drains it with a poll/batch loop (0.05 s idle poll — granularity is irrelevant against a 30 s flush interval). Closing the channel is the only shutdown signal: producer exit (deadline, fatal error, retry exhaustion, stop!) closes it, the sink drains what remains, flushes, and returns its count. Ctrl-C is converted to InterruptException (Base.exit_on_sigint(false)) and takes the same path — no data is lost on interrupt (tested).","title":"4. Concurrency model"},{"category":"section","location":"architecture/#5.-Failure-handling","page":"Architecture","text":"Reconnection: jittered exponential backoff (base·2^attempt, capped), attempt counter reset after any connection that delivered data; bounded by reconnect_max_retries.\nSession deadline: limits.max_session_hours is enforced by a guard task, so it also ends a connection that never drops; the reconnect loop alone would only test it between connections.\nFatal vs retryable: Alpaca error codes 401–411 (bad auth, connection limit, bad subscription) abort immediately — retrying them is a ban risk, not resilience. The fatal signal is carried out of the WebSocket handler by value, not by throw: exceptions crossing HTTP.jl's internal task boundary arrive wrapped, which would defeat isa dispatch (this was a real bug, caught by the mock-server tests).\nStale connections: HTTP.jl 1.x websockets have no read-idle timeout, so a watchdog task force-closes the socket after stream.stale_timeout_s without a frame, triggering the normal reconnect path.\nMarket-hours railings: /v2/clock gates connection (stream.require_market_open); stream.wait_for_open sleeps until the next open instead of exiting; stream.stop_at_market_close schedules a graceful stop at next_close so an unattended session doesn't idle on a dead overnight connection. Half-days and holidays are handled implicitly because the clock endpoint, not a local calendar, is authoritative (early-close behavior is tested end-to-end against a mock clock). Delayed feeds (delayed_sip, 15 min) shift both railings by the feed delay via feed_delay_ns: the open-wait extends past the silent post-open window and the close guard runs to next_close + delay + 60 s so the delayed tape tail is captured, not truncated.\nShutdown robustness: close(ws) is a handshake; a peer that never acks the CLOSE frame would hang the read loop, so stop! severs the raw transport after a 5 s grace if the graceful close has not completed.\nResource guards: free-disk check before and during a session (limits.min_free_disk_gb, mid-session breach stops the stream gracefully); compaction reroutes raw inputs whose estimated footprint exceeds limits.compact_mem_fraction of free RAM through bounded-memory spill compaction, which keeps at most limits.max_open_spill_files spill files open and so stays inside the file-descriptor limit however many symbol-days the input spans; a channel-occupancy warning fires when the sink lags the stream (>80% capacity).\nREST requests are bounded: every request carries a read and a connection timeout ([rest]), so a server that accepts a connection and goes silent cannot block a backfill for good. Timeouts, refused or dropped connections and 429/5xx are retried with exponential backoff, honoring Retry-After; anything else fails at once.\nNothing is process-global: a session runs under its own logger (with_logger), which it closes when it ends, and its stop flag is an atomic owned by the session and shared with that logger to mute transport teardown noise. The caller's global logger is untouched, and two sessions in one process neither share a log nor mute each other.\nA dead analysis tap cannot end persistence: tee drops an output its consumer closed, with a warning, and keeps feeding the others; the fan-out task is monitored, so its own failure is logged rather than lost.\nData integrity: compaction removes exact duplicate prints (reconnection double-delivery, overlapping backfill/live captures); session_report audits every capture for duplicates, exchange-time gaps, out-of-order delivery, and clock skew (negative receive latency) before the data is used for science.\nProvenance, crash-only: every stream/backfill session writes its <session_id>.meta.toml sidecar at START (status = \"running\"; git commit + dirty flag, versions, hostname, pid, hardware fingerprint — CPU model/cores, memory, thread and BLAS-thread counts, versioninfo — and the full config snapshot) and finalizes it at exit (completed / interrupted / failed, counts). A copy of the resolved Manifest.toml is written beside it as <session>.manifest.toml and named under provenance.manifest: no manifest is tracked in the repository, so this copy is what pins the dependency versions a session ran with. Startup reconciliation relabels sidecars of dead processes aborted and reports zero-byte raw stubs — provenance survives SIGKILL and power loss, which graceful-path handling alone cannot (threaded Ctrl-C delivery is best-effort in Julia).\nBackfill robustness: per-(symbol, trading-day) download loop with page-streamed sink writes — memory is bounded by one REST page plus the configured heap ceiling (limits.max_live_heap_mb, auto-GC then loud stop); backfill.resume skips pairs already processed, so a rerun after any abort resumes. Compaction spills to per-group files when the input exceeds the in-memory budget, bounding memory by the largest single (symbol, day) group. Those files go beside out_dir, never to tempdir(): the spill copies the whole input once, and /tmp is RAM on a systemd distribution, so the bounded-memory path would otherwise be the one that exhausts memory. The free space is checked before the first line is read.\nSingle-instance lock: a PID-file lock per data tree prevents two sessions from interleaving on one raw directory; stale locks from dead processes break automatically.","title":"5. Failure handling"},{"category":"section","location":"architecture/#6.-Dependency-decisions-(verified-2026-07-30)","page":"Architecture","text":"Choice Rationale\nHTTP.jl pinned to 1.x (resolved 1.11) 2.x is a weeks-old breaking rewrite (Reseau.jl backend). For unattended capture, battle-tested wins. Migration path documented: kwarg renames (readtimeout→read_idle_timeout etc.); our watchdog becomes redundant on 2.x.\nJSON3.jl over new JSON.jl 1.x JSON.jl 1.x rewrite benches ~10× slower for hot typed materialization; JSON3 is maintenance-mode but stable. Revisit when JSON.jl closes the gap.\nNo WebSockets.jl Abandoned (last release 2022). HTTP.WebSockets is the ecosystem standard.\nNo AlpacaMarkets.jl REST-only, single maintainer, pins that conflict with a modern stack. Direct HTTP+JSON is ~200 lines and fully under test.\nDotEnv.jl 1.0 (load!) Registered, stable, zero-issue scope.\nCustom ns timestamps over NanoDates.jl One regex + integer math, zero deps on the hot path; NanoDates remains an option as a display layer.\nHand-rolled backoff Base.retry/Retry.jl don't fit a stateful reconnect loop with attempt-reset semantics.\nPlotting as package extensions CairoMakie and UnicodePlots are weak dependencies. A consumer that feeds an estimator from Channel{Trade} should not install or precompile a plotting stack to do it; the figures load with using CairoMakie, the monitor's plots with using UnicodePlots.\nNo MathTeXEngine.jl dependency The Computer Modern faces come from Makie's theme_latexfonts(), which wraps the same fonts.","title":"6. Dependency decisions (verified 2026-07-30)"},{"category":"section","location":"architecture/#7.-Testing-strategy","page":"Architecture","text":"test/mock_alpaca.jl implements the Alpaca REST + WebSocket v2 protocol in-process (scriptable per-connection behavior: batches, then fatal codes), so the full pipeline — connect, auth, subscribe, stream, reconnect, fatal stop, batched persistence, compaction, replay — runs end-to-end in CI with no credentials and no network. Unit tests cover timestamp round-trips at every fractional width, offset normalization, config validation, sink rolling/never-reopen, corrupt-line recovery, safesave compaction, tee fan-out, and replay pacing.","title":"7. Testing strategy"},{"category":"section","location":"architecture/#8.-Visualization","page":"Architecture","text":"The figures are implemented in ext/MarketTickStreamerMakieExt.jl, a package extension triggered by CairoMakie; the package declares and documents the functions (src/figures.jl) and keeps the plotting-free numerics (src/diagnostics.jl): survival functions, tail-preserving thinning, min–max decimation, the Hill estimator.\n\nTwo figure families with identical styling (Computer Modern, boxed axes, no titles, HH:MM exchange-time axes, decade log ticks with 2×/5× intermediates and 10⁰ → 1, 10¹ → 10 collapse, in-axis count and fitted tail-exponent annotations):\n\nSession figures — one 2×2 panel per (symbol, trading day) from raw captures: price path (min–max decimated), trades per minute, and survival functions (CCDFs) of inter-arrival times and trade sizes on log-log axes — the distributions whose heavy tails matter first for exotic time series methods, and which linear histograms hide.\nOverview figures — one panel per symbol across many days from the processed tree: price on a concatenated trading-time axis (overnight gaps removed, session boundaries marked), a day × session-minute activity heatmap, and pooled intra-session waiting-time and size CCDFs — overnight gaps are excluded by construction because they would contaminate the waiting-time tail.\n\nCCDF point clouds are deterministically thinned (tail-preserving) and price paths min–max decimated, so million-tick figures stay vector-light. PDF + PNG (px_per_unit = 4) via scripts/visualize.jl (raw-file mode prints the session_report QA table; --overview sweeps the processed tree with symbol/date filters).","title":"8. Visualization"},{"category":"section","location":"architecture/#9.-Live-monitoring","page":"Architecture","text":"monitor.jl implements attach-mode monitoring: a separate read-only process tails the session's raw NDJSON part files (incremental offsets, torn-line carries, roll-aware) and renders an in-terminal dashboard: tick totals and rate, tape-head timestamp and lag, per-symbol counts. The dashboard is text; with UnicodePlots loaded a second package extension adds a rate-history plot and draws the counts as a bar chart (headless-safe). Attaching from outside the producer was chosen over an in-process panel because it cannot interfere with the capture, its logging, or its progress output (a §9 requirement of the project standards), it works identically for live capture and backfill, and it can attach to or detach from a session at any point — including sessions started by another process. Refresh cadence and panel sizing come from the [monitor] config table; scripts/monitor.jl is the entry point.","title":"9. Live monitoring"},{"category":"section","location":"replay/#Replay-and-analysis-interfaces","page":"Replay & Analysis Interfaces","text":"Analysis pipelines are external to this package. What it owes them is a stream whose contract is written down, and a way to develop against that stream without a market being open. This page is that contract.\n\nThe pipelines this was built to feed — exact waiting-time distributions of scalar series, long-memory estimation, criticality in tick arrival — live in separate packages of their own, not yet released. Nothing here depends on them, and the contract below is what any consumer gets. Two worked consumers ship under examples/, and the test suite executes both, so they cannot drift from what this page promises.\n\n(Image: A recorded session replayed tick by tick, the price path drawing itself against exchange time)","title":"Replay and analysis interfaces"},{"category":"section","location":"replay/#One-interface,-two-sources","page":"Replay & Analysis Interfaces","text":"live_source and replay_source both hand back a Channel{Trade}. A consumer written against one works unchanged against the other, which is the point: a real-time method is developed and validated on recorded flux — reproducible, repeatable, free, available at three in the morning — and only then pointed at the wire.\n\nusing MarketTickStreamer\n\n# Recorded session, re-emitted at the original inter-arrival times.\nch = replay_source(\"data/raw/session_part001.jsonl\")\n\nfor trade in ch                      # terminates when the recording ends\n    # ... the analysis method under test\nend\n\npace = \"max\" drops the pacing and emits as fast as the consumer takes them, which is the mode for throughput work; speed compresses recorded time, so speed = 60.0 replays an hour of tape in a minute.\n\nA session is replayed from its raw files, which are read whole. A corpus is replayed from the processed tree, and streamed: the files of one trading day — one per symbol — are loaded, merged on the clock and emitted before the next day is opened, so a symbol-year replays in the memory of its busiest day.\n\nfiles = processed_files(\"data/processed\", \"AAPL\"; from = Date(2026, 5, 1))\nfor trade in replay_source(files; pace = \"max\")\n    # ... an estimator run over months of tape\nend\n\nSix AAPL days, 5.6 million prints, go through at about a million prints per second with the resident set flat at 1.2 GiB from the first day to the last. Recorded pace keeps one schedule across the days, closures included, so replaying a week at speed = 1 takes a week. If a later day cannot be read, the channel closes with that error and the consumer's loop rethrows it, after every earlier print has been delivered.","title":"One interface, two sources"},{"category":"section","location":"replay/#What-a-Trade-means","page":"Replay & Analysis Interfaces","text":"Field Type Meaning\nsymbol String Ticker as the venue reports it.\ntime_ns Int64 Exchange timestamp, nanoseconds since the UNIX epoch (UTC).\nrecv_ns Int64 Local receipt timestamp, same units. Zero for backfilled prints — they never crossed the wire.\nprice Float64 Trade price.\nsize Float64 Trade size. Float rather than integer because fractional-share prints exist.\nexchange String Reporting venue code.\nconditions Vector{String} Sale-condition codes as reported; not filtered.\ntape String Consolidated tape identifier (A, B, C).\nid Int64 Venue trade identifier, unique per venue and day.\n\nTwo clocks per print is a deliberate choice: with time_ns and recv_ns both recorded, feed latency is a measurable quantity afterwards rather than an assumption baked in at capture time.\n\nTrade carries value semantics — == and hash compare fields, not the identity of the conditions vector — so round-tripping through the raw layer produces objects that compare equal.","title":"What a Trade means"},{"category":"section","location":"replay/#Guarantees","page":"Replay & Analysis Interfaces","text":"Ordering. Replay emits in ascending order of its clock, sorted on load: recv_ns when every record carries one, time_ns otherwise (clock = \"auto\"; either can be forced). Backfilled records have recv_ns = 0, so a backfilled recording replays on exchange time.\n\nPacing. Each emission is scheduled against the absolute time elapsed since the first record, not slept gap by gap. sleep overshoots by about a millisecond, so per-gap sleeping stretches a session whose gaps are of that order — 65 % on gaps of 2.5 ms — whereas on an absolute schedule the error stays within one overshoot however long the replay runs. Gaps below a millisecond are emitted back to back, in order. The live source emits in arrival order, which is not the same as ascending time_ns: a consolidated tape interleaves venues, and late prints are normal. Any method that needs monotone exchange time must sort or reject explicitly; session_report quantifies how often it happens in a given capture.\n\nCompletion. Both sources close the channel when the stream ends — the recording exhausted, the session deadline reached, the market closed, a fatal protocol error. A for t in ch loop therefore terminates on its own, and a consumer needs no separate shutdown signal.\n\nBackpressure. The channel is bounded (limits.channel_capacity). A slow consumer blocks the producer, and on a live session that propagates into TCP backpressure rather than into unbounded memory growth. This is why the default is to block: silently dropping ticks would corrupt exactly the arrival statistics the package exists to measure.\n\nDeterminism. Replaying the same files with pace = \"max\" yields the identical sequence every time. With pace = \"recorded\" the sequence is identical but the timing is approximate: gaps under a millisecond are not resolvable by sleep, so they are emitted back to back.","title":"Guarantees"},{"category":"section","location":"replay/#Fanning-out-to-several-consumers","page":"Replay & Analysis Interfaces","text":"tee splits one stream into independent channels, with a per-output overflow policy:\n\nsession = live_source(provider, cfg)\npersist, analyse = tee(session.channel, 2; lossy = [false, true])\n\nA non-lossy output blocks the fan-out when full — use it for persistence, which must always win. A lossy output drops incoming ticks instead of stalling the capture, counts the drops, and reports them through on_drop. An analysis tap that occasionally cannot keep up belongs on a lossy output; one whose results depend on seeing every print does not, and should instead run offline against the raw files, where nothing is ever dropped.","title":"Fanning out to several consumers"},{"category":"section","location":"replay/#A-worked-consumer","page":"Replay & Analysis Interfaces","text":"examples/waiting_times.jl is this page in code: it replays a recorded session, splits it with tee into a lossless consumer and a lossy analysis tap, accumulates inter-arrival times in a single pass, and stops when the channel closes — no shutdown protocol of its own.\n\njulia examples/waiting_times.jl data/raw/<session>_part001.jsonl\n\nIt reports both populations side by side, which is the cheapest way to see what the choice costs on your own data. One AAPL session, 1 225 831 prints:\n\nevery execution          n=  1225830  mean=  0.0470 s  median=  0.0007 s  p99=  0.6231 s  max=   30.080 s\nprice-forming only       n=   408793  mean=  0.1409 s  median=  0.0005 s  p99=  1.4756 s  max=  116.269 s\n\nA median of 0.7 ms against a mean of 47 ms is the distribution announcing itself. Note also that excluding odd lots raises the mean roughly threefold while leaving the median where it was: it thins the dense clusters and stretches the tail rather than rescaling the whole distribution.\n\nBoth tee outputs in the example are lossless, deliberately. At maximum replay rate a lossy tap lost 85 000 of those prints — and it does not thin a sample evenly, it removes the bursts, which is where a waiting-time distribution lives. Backpressure costs nothing offline, since a blocked output only slows the replay.\n\nThe suite executes the example, so it cannot drift from the interface it documents.","title":"A worked consumer"},{"category":"section","location":"replay/#Which-prints-count-as-trades","page":"Replay & Analysis Interfaces","text":"conditions is reported, never acted on at capture. Whether a print belongs in your sample depends on the question, and the two questions this package serves want opposite answers:\n\nA price path — returns, volatility, Hurst or DFA estimation — wants price-forming prints only. Odd lots, contingent and derivatively priced trades and corrections carry prices that were never a consolidated last sale, and differencing them manufactures volatility nobody could trade on.\nAn arrival process — waiting-time distributions, tick intensity, criticality — wants every execution. An odd lot is a real fill by a real participant at a real instant, and dropping it distorts the very law under study.\n\nSo price_forming and filter_price_forming operate on loaded prints, and session_report reports n_trades beside n_price_forming so the size of the distinction is measured per capture rather than assumed. The gap is dominated by odd lots and grows with share price, so it varies strongly across a basket.\n\nThe default population, NON_PRICE_CONDITIONS, follows the tape plans' own sale-condition matrices: a code is excluded when the plan does not let it update the consolidated last sale, or lets it only when it is the sole qualifying trade of the day. That takes out odd lots, average-price prints (B on tapes A and B, W on tape C), prices fixed earlier or elsewhere (P, 4), late reports (Z, U), contingent trades and non-regular settlement. Extended-hours prints (T) stay in, because at tick resolution they are the price path of their session. Average-price and derivatively priced prints are few — about 3 % of what otherwise passes on a liquid Nasdaq name — but they are the ones that land percents away from the market: on one AAPL session they moved the day's price-forming minimum from 316.00 to 304.35. The lists are configuration ([quality.non_price_conditions]), so the population a session was reported under is recorded in its sidecar.\n\nState which of the two populations a result used. After the fact, a price series does not reveal it.","title":"Which prints count as trades"},{"category":"section","location":"replay/#Sampling-on-a-clock-that-runs-with-the-market","page":"Replay & Analysis Interfaces","text":"Market activity is not uniform in time, so sampling a price path every minute draws unevenly from the process that generates it — densely at the open, sparsely at lunch. tick_bars, volume_bars and dollar_bars resample a capture onto clocks that advance with activity instead: one bar per fixed count of prints, of shares, or of traded value.\n\ntrades = read_raw(files)\nbars = dollar_bars(filter_price_forming(trades), 5.0e6)   # one bar per $5M traded\n\nThe empirical motivation is old and well tested: price changes sampled in transaction time are far closer to independent and normal than calendar-time changes (Mandelbrot & Taylor 1967; Clark 1973; Ané & Geman 2000). Of the three, the value clock is the one that survives a change of scale — it is invariant to splits and roughly invariant to price drift, so a threshold chosen on one sample still means something on another.\n\nTwo choices are yours and are not incidental. Whether to filter to price-forming prints first changes the bar count by a factor of three on a high-priced name. And the bars carry recv_ns = 0, the same marker the raw layer uses for records that never crossed the wire, because a derived bar has no receipt time.\n\nA Bar records both ends of its interval: time_ns is the timestamp of its first print and close_ns that of the print that completed it. On an activity clock the amount transacted per bar is fixed and the duration is what varies, so close_ns - time_ns is itself a waiting time — that of a fixed quantity of activity rather than of a single print:\n\ndurations_s = [(b.close_ns - b.time_ns) / 1e9 for b in bars]\n\nConsecutive bars never overlap, bars[i].close_ns <= bars[i+1].time_ns, and the gap between them is the wait for the next bar's first print.","title":"Sampling on a clock that runs with the market"},{"category":"section","location":"replay/#Working-from-the-file-layer-instead","page":"Replay & Analysis Interfaces","text":"For methods that need the whole record rather than a stream — tail exponents, long-memory estimation, anything needing the sample in memory — read_raw returns a Vector{Trade} from raw NDJSON, and compact_raw writes per-symbol per-day CSV or Arrow that any other tool can read. The raw layer is the source of truth; compaction is a convenience over it, never a replacement.","title":"Working from the file layer instead"},{"category":"section","location":"api_analysis/#API-reference:-analysis-interfaces","page":"Analysis interfaces","text":"Continues the acquisition and storage reference: what operates on a loaded or replayed capture.","title":"API reference: analysis interfaces"},{"category":"section","location":"api_analysis/#Data-quality-and-resource-guards","page":"Analysis interfaces","text":"","title":"Data quality and resource guards"},{"category":"section","location":"api_analysis/#Resampling-onto-activity-clocks","page":"Analysis interfaces","text":"","title":"Resampling onto activity clocks"},{"category":"section","location":"api_analysis/#Replay","page":"Analysis interfaces","text":"","title":"Replay"},{"category":"section","location":"api_analysis/#Diagnostic-figures","page":"Analysis interfaces","text":"Declared and documented in the package, implemented by a package extension that loads with CairoMakie: using MarketTickStreamer, CairoMakie. Without it a call ends in a MethodError that names the package to load.","title":"Diagnostic figures"},{"category":"section","location":"api_analysis/#Numerics-behind-the-figures","page":"Analysis interfaces","text":"Plotting-free, and usable on their own.","title":"Numerics behind the figures"},{"category":"constant","location":"api_analysis/#MarketTickStreamer.NON_PRICE_CONDITIONS","page":"Analysis interfaces","text":"NON_PRICE_CONDITIONS\n\nSale-condition codes that disqualify a print from setting a price, keyed by tape: \"A\" and \"B\" are CTA-processed, \"C\" is UTP-processed, \"O\" is the OTC tape. The same character means different things on different tapes — the average price trade is B on CTA and W on UTP, where B is a bunched trade — which is why the lists are not shared.\n\nThe lists for A, B and C are derived from the plans' own sale-condition matrices (CTS Pillar output specification 2.11b, UTP data feed services specification 4.1): a code is listed when its consolidated update-last entry is \"no\", or \"no unless it is the only qualifying trade of the day\". That covers prints that carry no current price by construction — averages (B, W), prices fixed earlier or elsewhere (P, 4), late reports (Z, G, U), non-regular settlement (C, R, N), contingent trades (V, 7), price variation (H), market-center official open and close (Q, M) — and odd lots (I). On 40 symbol-days captured with this package the stale-price classes among them (W, 4, P, Z) sit one to two orders of magnitude further from the last regular print than regular prints do; odd lots do not, and are listed because the plans bar them. The provider delivers the plans' codes unchanged, and its condition_map returns their glossary.\n\nTwo deliberate departures from the matrices. T (extended hours) is not listed although the plans keep it from the consolidated last, which is a regular-session statistic: at tick resolution an extended-hours print is the price path of its session. 9 (corrected consolidated close) is listed although the plans let it update the last: at tick resolution it is a correction message rather than an execution, though for a daily bar it is precisely the official close.\n\nThe O list is the provider's published one and has not been checked against a specification.\n\nThis is not a per-field eligibility table — the plans track high/low, open, last and volume eligibility separately, and a print may update some and not others. It answers one question, the one a price path needs answered: may this print set a price.\n\n\n\n\n\n","title":"MarketTickStreamer.NON_PRICE_CONDITIONS"},{"category":"method","location":"api_analysis/#MarketTickStreamer.check_live_heap-Tuple{Real}","page":"Analysis interfaces","text":"check_live_heap(limit_mb; context = \"\") -> Nothing\n\nConfig-gated RAM ceiling: if the live heap exceeds limit_mb, force a garbage collection; if it still exceeds the ceiling, fail loudly with a message naming context — a graceful stop beats an OOM kill. Call from long accumulation loops (backfill pages, compaction groups).\n\n\n\n\n\n","title":"MarketTickStreamer.check_live_heap"},{"category":"method","location":"api_analysis/#MarketTickStreamer.coverage_report-Tuple{AbstractVector{<:AbstractString}, Any}","page":"Analysis interfaces","text":"coverage_report(paths, provider; feed = \"sip\", page_limit = 10_000,\n                rate_sleep_s = 0.35) -> DataFrame\n\nCompare a live capture against the historical tape: for every symbol in the raw NDJSON paths, count captured trades (after exact-duplicate removal) and query the provider's historical trade count over the same inclusive exchange-time window. coverage = captured / reference; values below 1 quantify feed coverage and stream drops, values above 1 indicate duplicate or spurious prints that dedup did not catch.\n\n\n\n\n\n","title":"MarketTickStreamer.coverage_report"},{"category":"method","location":"api_analysis/#MarketTickStreamer.deduplicate_trades-Tuple{AbstractVector{Trade}}","page":"Analysis interfaces","text":"deduplicate_trades(trades) -> Vector{Trade}\n\nRemove exact duplicate prints (same symbol, exchange timestamp, id, price, size, exchange), keeping first occurrence and preserving order. Guards against reconnection double-delivery and overlapping backfill/live captures.\n\n\n\n\n\n","title":"MarketTickStreamer.deduplicate_trades"},{"category":"method","location":"api_analysis/#MarketTickStreamer.filter_price_forming-Tuple{AbstractVector{Trade}}","page":"Analysis interfaces","text":"filter_price_forming(trades; non_price = NON_PRICE_CONDITIONS) -> Vector{Trade}\n\nKeep only the prints for which price_forming holds, preserving order. Use on a loaded capture, never on the way in.\n\n\n\n\n\n","title":"MarketTickStreamer.filter_price_forming"},{"category":"method","location":"api_analysis/#MarketTickStreamer.free_disk_gb-Tuple{AbstractString}","page":"Analysis interfaces","text":"free_disk_gb(path) -> Float64\n\nFree disk space (GiB) on the filesystem containing path.\n\n\n\n\n\n","title":"MarketTickStreamer.free_disk_gb"},{"category":"method","location":"api_analysis/#MarketTickStreamer.observed_round_lot-Tuple{AbstractVector{Trade}}","page":"Analysis interfaces","text":"observed_round_lot(trades; code = \"I\") -> Float64\n\nThe venue's round lot for these prints, read off the tape: one share more than the largest trade still flagged as an odd lot. NaN when no print carries the flag.\n\nWorth reporting per capture because the round lot is neither 100 shares nor constant. Under the SEC Market Data Infrastructure rules it is tiered by share price — 100 shares at or below 250 USD, 40 up to 1000 USD, 10 up to 10 000 USD, 1 above — and reassigned semiannually per symbol from that symbol's average close over an evaluation month, effective the first business day of May and of November.\n\nThe consequence is that the odd-lot flag, and therefore the price-forming population, silently changes membership at those dates. On a 251-day AAPL capture taken with this package, the lot fell from 100 to 40 shares on 2026-05-01 and the median inter-arrival time of the price-forming population dropped by a factor of eight across the boundary, with no change in market behaviour. A symbol near a tier threshold can also oscillate: over the same span ERIE went 100, 40, then 100 again inside eight months.\n\nReporting it turns an invisible redefinition into an observable, and it costs one pass over prints already in memory.\n\n\n\n\n\n","title":"MarketTickStreamer.observed_round_lot"},{"category":"method","location":"api_analysis/#MarketTickStreamer.price_forming-Tuple{Trade}","page":"Analysis interfaces","text":"price_forming(t::Trade; non_price = NON_PRICE_CONDITIONS) -> Bool\n\nWhether t may set a price, i.e. whether none of its sale conditions is listed for its tape in non_price. A print carrying several conditions is disqualified by any one of them, which is the precedence rule both tape plans state: a single \"does not update\" overrides every permissive condition beside it.\n\nA print whose tape has no entry in non_price is kept. An unknown tape means no basis on which to exclude, and silently discarding prints on that ground would be the worse error; session_report counts what survives, so the effect stays visible.\n\nThis is an analysis-time predicate. Capture is never filtered — the raw layer records every print the venue reported, and which subset constitutes \"a trade\" is a decision each analysis makes for itself. It is also not one decision: a price path wants price-forming prints only, whereas an arrival process or a waiting-time distribution counts every execution, odd lots and contingent trades included. Filtering the capture would foreclose the second question to answer the first.\n\n\n\n\n\n","title":"MarketTickStreamer.price_forming"},{"category":"method","location":"api_analysis/#MarketTickStreamer.session_report-Tuple{AbstractVector{<:AbstractString}}","page":"Analysis interfaces","text":"session_report(paths; gap_threshold_s = 60.0) -> DataFrame\n\nAudit raw session files and return one row per symbol:\n\nn_trades, n_duplicates (exact duplicate prints)\nn_price_forming (prints that may set a price, see price_forming; the difference from n_trades is odd lots, contingent and derivatively priced trades, corrections and the like — present in the tape, and in the arrival process, but not in a price path)\nfirst_time, last_time (UTC, ms precision, from exchange timestamps)\nn_out_of_order (exchange timestamps decreasing in arrival order)\nmax_gap_s / n_gaps (largest / count of exchange-time gaps exceeding gap_threshold_s — judge against the instrument's typical activity; quiet symbols gap naturally)\nmedian_latency_ms / n_negative_latency (receive minus exchange time, live-captured rows only; negative values indicate clock skew. NaN when the file is pure backfill)\nround_lot (the venue's round lot, read off the tape by observed_round_lot; it is price-tiered and reassigned semiannually, so it is not 100 and does not hold still)\n\nInspect this before trusting any captured session.\n\n\n\n\n\n","title":"MarketTickStreamer.session_report"},{"category":"method","location":"api_analysis/#MarketTickStreamer.dollar_bars-Tuple{AbstractVector{Trade}, Real}","page":"Analysis interfaces","text":"dollar_bars(trades, value; keep_partial = false) -> Vector{Bar}\n\nResample trades onto a traded-value clock: one Bar per value of price times size.\n\nThis is the clock that survives changes of scale. It is invariant to splits, and roughly invariant to price-level drift, so a threshold chosen on one sample stays meaningful on another — the property neither tick_bars nor volume_bars has, and the reason dollar bars are the usual default for cross-sample work.\n\nvalue is in the price's own units; no currency conversion is performed.\n\n\n\n\n\n","title":"MarketTickStreamer.dollar_bars"},{"category":"method","location":"api_analysis/#MarketTickStreamer.tick_bars-Tuple{AbstractVector{Trade}, Integer}","page":"Analysis interfaces","text":"tick_bars(trades, n; keep_partial = false) -> Vector{Bar}\n\nResample trades onto a transaction clock: one Bar per n prints.\n\nThe count is of prints as recorded, so whether odd lots and other non-price-forming records count towards it is decided before the call — pass filter_price_forming(trades) to exclude them. On a high-priced name that choice changes the bar count by a factor of three, so it is not incidental.\n\nSampling by transaction count is the oldest of these clocks: Mandelbrot and Taylor (1967) doi:10.1287/opre.15.6.1057 proposed price changes as a subordinated process running on transaction time, and Ané and Geman (2000) doi:10.1111/0022-1082.00286 showed returns sampled this way are close to normal where calendar-time returns are heavy-tailed.\n\nPrints are ordered by exchange timestamp, not arrival. The print that completes a bar belongs to it. Its timestamp is the bar's close_ns, so consecutive bars satisfy bars[i].close_ns <= bars[i+1].time_ns. A trailing incomplete bar is dropped unless keep_partial, since its threshold — and therefore its comparability to the others — is not met.\n\ntick_bars(trades, 500)                      # one bar per 500 prints\ntick_bars(filter_price_forming(trades), 500)  # price-forming prints only\n\n\n\n\n\n","title":"MarketTickStreamer.tick_bars"},{"category":"method","location":"api_analysis/#MarketTickStreamer.volume_bars-Tuple{AbstractVector{Trade}, Real}","page":"Analysis interfaces","text":"volume_bars(trades, volume; keep_partial = false) -> Vector{Bar}\n\nResample trades onto a volume clock: one Bar per volume shares.\n\nClark (1973) doi:10.2307/1913889 introduced volume as the directing process for speculative prices, giving a finite-variance alternative to the stable-Paretian account of heavy tails: the unconditional return distribution is heavy-tailed because it mixes over a random volume clock, not because the underlying increments are.\n\nThe caveat is mechanical: a share is not a fixed unit of economic activity across time or across instruments. Share prices drift, splits reset the scale, and a fixed share threshold therefore samples a different amount of value in January than in December. dollar_bars is the usual remedy.\n\n\n\n\n\n","title":"MarketTickStreamer.volume_bars"},{"category":"method","location":"api_analysis/#MarketTickStreamer.replay_source-Tuple{AbstractVector{<:AbstractString}}","page":"Analysis interfaces","text":"replay_source(paths; pace = \"recorded\", speed = 1.0, clock = \"auto\",\n              capacity = 100_000) -> Channel{Trade}\n\nCreate a channel that replays recorded trades, ordered by the replay clock. paths are either raw NDJSON files (.jsonl) or processed day files (YYYY-MM-DD.csv|.arrow, e.g. from processed_files); the two kinds cannot be mixed.\n\nRaw files are read whole and sorted, since arrival order is not chronological across venues and parts. This is the mode for a session.\nProcessed files are streamed one trading day at a time: the files of a date — one per symbol — are loaded, merged on the replay clock and emitted before the next date is opened, so memory is bounded by the busiest day however long the span. This is the mode for a corpus. Recorded pace runs on one schedule across the days, closures included: replaying a week at speed = 1 takes a week, so pass speed, or pace = \"max\".\npace = \"recorded\": honor the original inter-arrival times of the replay clock against an absolute schedule, compressed by speed (e.g. speed = 60.0 replays an hour in a minute), so the timing error does not accumulate over a session.\npace = \"max\": emit as fast as the consumer takes them (throughput mode).\n\nThe replay clock is selected by clock:\n\n\"recv\": local receive time — what a live consumer experienced, network jitter included. Throws if any record lacks it.\n\"exchange\": the venue's own timestamps — the only clock a backfilled recording has.\n\"auto\" (default): \"recv\" when every record carries a receive timestamp, otherwise \"exchange\", logged at @info. For processed files the choice is made on the first day and held.\n\nThe channel closes when the recording is exhausted, which cleanly terminates any consumer written against run_sink!-style loops. An error while streaming a later day (an unreadable file, a missing receive clock) closes the channel with that error, and the consumer's iteration rethrows it.\n\n\n\n\n\n","title":"MarketTickStreamer.replay_source"},{"category":"function","location":"api_analysis/#MarketTickStreamer.overview_figure","page":"Analysis interfaces","text":"overview_figure(symbol, days; tz = tz\"America/New_York\",\n                non_price = NON_PRICE_CONDITIONS,\n                session = (9.5, 16.0),\n                price_unit = \"USD\", size_unit = \"shares\") -> Figure\n\nMulti-day diagnostic figure for one symbol from per-day processed tables (days is a vector of (date, DataFrame) pairs, sorted internally by date):\n\nprice path on a concatenated trading-time axis (overnight gaps removed, session boundaries dashed, days labeled at their centers), drawn from the prints that price_forming admits under non_price,\nday × session-minute activity heatmap,\npooled intra-session inter-arrival CCDF (overnight gaps excluded by construction — they would contaminate the waiting-time tail; with a 24-hour session midnight is not a boundary, and the wait across it is kept whenever the next calendar day is present),\npooled trade-size CCDF with fitted tail exponent.\n\nsession is the venue's regular session as exchange-local hours (open, close), 0 <= open < close <= 24; it sets the width of a day on the concatenated axis and the span of the activity heatmap. The default is the US equity session; a venue that never closes takes (0.0, 24.0). price_unit and size_unit name the units on the price and size axes. All three are properties of the venue and come with its ProviderSpec.\n\nImplemented by the CairoMakie extension: using CairoMakie first.\n\n\n\n\n\n","title":"MarketTickStreamer.overview_figure"},{"category":"function","location":"api_analysis/#MarketTickStreamer.save_overview_figures","page":"Analysis interfaces","text":"save_overview_figures(processed_dir, out_dir;\n                      symbols = nothing, from = nothing, to = nothing,\n                      formats = (\"pdf\", \"png\"), tz = tz\"America/New_York\",\n                      session = (9.5, 16.0), min_days = 2,\n                      price_unit = \"USD\", size_unit = \"shares\") -> Vector{String}\n\nRender one multi-day overview_figure per symbol from the processed tree (processed_dir/SYMBOL/YYYY-MM-DD.csv|.arrow; safesave _#N backups and .partial files are ignored — the base file per day is authoritative and always the newest). symbols, from, and to restrict the sweep. Output: out_dir/SYMBOL_<from>_<to>.pdf|png (safesave). Days are loaded one symbol at a time, so memory stays bounded by one symbol's span. session is passed to overview_figure.\n\nImplemented by the CairoMakie extension: using CairoMakie first.\n\n\n\n\n\n","title":"MarketTickStreamer.save_overview_figures"},{"category":"function","location":"api_analysis/#MarketTickStreamer.save_session_figures","page":"Analysis interfaces","text":"save_session_figures(raw_paths, out_dir;\n                     formats = (\"pdf\", \"png\"), tz = tz\"America/New_York\",\n                     min_trades = 10,\n                     price_unit = \"USD\", size_unit = \"shares\") -> Vector{String}\n\nRender one diagnostic figure per (symbol, trading day) found in the raw NDJSON raw_paths, saved as out_dir/SYMBOL_YYYY-MM-DD.pdf|png (safesave — the new figure takes the canonical name, a displaced one is kept as _#N). Groups with fewer than min_trades ticks are skipped with an @info (distribution panels are meaningless). PNG output is written at px_per_unit = 4. Returns the files written.\n\nImplemented by the CairoMakie extension: using CairoMakie first.\n\n\n\n\n\n","title":"MarketTickStreamer.save_session_figures"},{"category":"function","location":"api_analysis/#MarketTickStreamer.session_figure","page":"Analysis interfaces","text":"session_figure(trades; tz = tz\"America/New_York\",\n               non_price = NON_PRICE_CONDITIONS,\n               price_unit = \"USD\", size_unit = \"shares\") -> Figure\n\nBuild the 2×2 diagnostic figure for one symbol's single-day ticks:\n\nprice path over exchange-local time (HH:MM axis, min–max decimated), drawn from the prints that price_forming admits under non_price, with the admitted share annotated,\nactivity (trades per minute, every print),\ninter-arrival time survival function P(Δt > x) (log-log, decade ticks) with the median annotated,\ntrade-size survival function P(S > s) with a Hill tail exponent.\n\ntrades must be non-empty and single-symbol (as produced by the grouping in save_session_figures); they are sorted internally by exchange time. price_unit and size_unit name the units on the price and size axes; the provider knows them (ProviderSpec).\n\nImplemented by the CairoMakie extension: using CairoMakie first.\n\n\n\n\n\n","title":"MarketTickStreamer.session_figure"},{"category":"function","location":"api_analysis/#MarketTickStreamer.tick_theme","page":"Analysis interfaces","text":"tick_theme() -> Theme\n\nPublication defaults: Computer Modern fonts, 26 pt labels over 22 pt ticks, boxed axes with 1.5-wide spines, inward ticks, no minor ticks, faint dashed grey grid, 3-wide data lines, frameless horizontal legends.\n\nImplemented by the CairoMakie extension: using CairoMakie first.\n\n\n\n\n\n","title":"MarketTickStreamer.tick_theme"},{"category":"method","location":"api_analysis/#MarketTickStreamer._pooled_gaps-Tuple{AbstractVector{<:Tuple{Dates.Date, DataFrames.DataFrame}}}","page":"Analysis interfaces","text":"_pooled_gaps(days; continuous) -> (gaps, excluded)\n\nWaiting times [s] pooled over per-day processed tables (date, DataFrame), sorted by date, and the number of between-day gaps left out.\n\nWithin a day every gap counts. Between two days the gap spans a closure and is not a waiting time of the arrival process, so it is dropped. On a venue that never closes (continuous = true) midnight is not a boundary: the wait across it is kept whenever the next calendar day is present, and only gaps across missing days are dropped.\n\n\n\n\n\n","title":"MarketTickStreamer._pooled_gaps"},{"category":"method","location":"api_analysis/#MarketTickStreamer._tail_fit-Tuple{Vector{Float64}}","page":"Analysis interfaces","text":"_tail_fit(x; frac = 0.1) -> Union{Nothing,NamedTuple}\n\nHill estimator of the tail exponent α in P(X > x) ∝ x^(-α) from the k largest of the ascending-sorted positive samples x, with k the top frac of the sample and at least 30:\n\nα̂ = k / Σᵢ ln(x₍ₙ₋ᵢ₊₁₎ / x₍ₙ₋ₖ₎),    σ = α̂ / √k.\n\nThis is the maximum-likelihood estimator of a Pareto tail above the threshold x₍ₙ₋ₖ₎ (Hill 1975). A least-squares line through the log-log survival function is not a substitute: its points are cumulative and therefore strongly correlated, so the slope is biased and its regression standard error is too small by a large factor (Clauset, Shalizi & Newman 2009).\n\nThe threshold is a fixed fraction rather than a fitted x_min, and trade sizes are discrete with mass at round numbers, so the value is a diagnostic of tail weight, not a measurement of a power law. Returns nothing for fewer than 50 samples, for a tail spanning less than a factor of two, or when the tail is degenerate.\n\n\n\n\n\n","title":"MarketTickStreamer._tail_fit"},{"category":"section","location":"#MarketTickStreamer","page":"Home","text":"Provider-agnostic tick-by-tick market data acquisition in pure Julia: live WebSocket streaming, historical REST backfill, append-only raw persistence, compaction to analysis-ready files, and paced replay of recorded sessions.\n\n(Image: Per-session diagnostic: price path, trade rate, and the survival functions of inter-arrival time and trade size)\n\nOne trading day of one symbol, as scripts/visualize.jl renders it. The inter-arrival distribution spans seven decades; the size distribution carries a fitted tail exponent.","title":"MarketTickStreamer"},{"category":"section","location":"#Purpose","page":"Home","text":"The package exists to supply a real-time data flux for testing time series analysis methods — waiting-time distributions, long-memory estimation, physics-informed models, criticality in tick arrival — on a stream whose statistics nobody controls. It therefore ends where analysis begins: it delivers ordered, deduplicated, provenance-tagged prints through a Channel{Trade} and a file layer, and implements no estimators of its own.\n\nThat boundary is deliberate. An acquisition layer that also computes is an acquisition layer whose numbers cannot be audited independently of the method under test.","title":"Purpose"},{"category":"section","location":"#Status","page":"Home","text":"Validated against the live consolidated tape on 2026-08-06: a three-hour capture of 3.17 M prints across 24 symbols, 533 MB of raw NDJSON, with no duplicate, out-of-order or missing record. Per-symbol coverage was cross-checked against the historical SIP tape and matched exactly for 22 of 24 symbols, and to better than 99.9996 % for the remaining two — evidence that Alpaca's free delayed_sip feed carries the complete consolidated tape, merely 15 minutes late, which for offline science is no delay at all.","title":"Status"},{"category":"section","location":"#Installation","page":"Home","text":"Requires Julia ≥ 1.12. The package is not registered; clone the repository for the full pipeline workflow (entry-point scripts, configuration, benchmarks, diagnostics), which is how it is meant to be used:\n\nusing Pkg\nPkg.activate(\".\")\nPkg.instantiate()","title":"Installation"},{"category":"section","location":"#Acquisition-model","page":"Home","text":"Three properties shape everything else:\n\nCrash-only. The raw layer is append-only NDJSON, one normalized Trade per line, flushed on a timer and a count. A killed process loses at most the unflushed tail; the next start reconciles the session sidecars. There is no shutdown path whose failure can lose data, because there is no shutdown path that must succeed.\nNanosecond integers. Timestamps are Int64 nanoseconds since the UNIX epoch from the wire to the file, never floating point, never a locale-dependent string.\nOne tape, two clocks. Every print carries both the exchange timestamp and the local receipt timestamp, so feed latency is measurable after the fact rather than assumed.","title":"Acquisition model"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"Architecture — data flow, persistence layers, concurrency, failure handling, and the reasoning behind each dependency.\nUsage & Configuration — the entry points and every key of config/config.toml.\nReplay & Analysis Interfaces — how an external analysis pipeline consumes live or recorded ticks, and what is guaranteed about ordering, completion and backpressure.\nAPI reference — the public functions and types: acquisition and storage, analysis interfaces.","title":"Where to go next"}]
}
